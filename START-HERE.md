# 🚀 新 AI 接手入口 — 先读这里

> **本文件是 JSK管理 项目给新接手 AI 的唯一入口。**
> 只需按顺序读完前两步，即可无缝续写本项目。
> 最后更新：2026-09-21（按 [`docs/文档整理规则.md`](docs/文档整理规则.md) 完成一次全仓库整理：统计口径对账 + 索引补登记）

---

## 📖 必读顺序

| 顺序 | 文件 | 作用 |
|------|------|------|
| 1️⃣ | **`START-HERE.md`**（本文件） | 入口与铁律 |
| 2️⃣ | **`AI交接指导.md`** | ✅ **唯一必读主文档**：项目速览、架构、标准工作流、铁律、高频缺陷地图 |
| 3️⃣ | `docs/bugs/README.md` | BUG 总索引（133 条）—— 动工前按领域查老坑，避免重复踩 |
| 4️⃣ | `docs/技术支持/README.md` | 代码片段 / 实测技术数据 / 外部 API 参考 / 探针脚本速查 |
| 5️⃣ | `docs/发布/一条龙-发布流程.md` | **发版必读**（含产物校验、OTA 验证、回滚、检查单） |
| 6️⃣ | `README.md` / `RELEASE_NOTES.md` | 功能与下载说明 / 对外更新日志（**只写用户可感知内容**） |
| 7️⃣ | `CHANGELOG.md` | 内部版本技术总表（改了什么、怎么验证的） |

> 🤖 **其它 AI 工具入口**：[`AGENTS.md`](AGENTS.md) 与 [`.github/copilot-instructions.md`](.github/copilot-instructions.md) —— 两者都是「先读 START-HERE → AI交接指导」的指路文件，内容一致、保持同步。

> 💡 文档已按「指导 / BUG / 技术支持 / 发布 / 规格与计划」五类重组，**旧文件已全部并入并删除**（不再保留归档目录）。
> 文档总地图见 [`docs/README.md`](docs/README.md)。

---

## ⚡ 30 秒速览

| 项 | 值 |
|---|---|
| 项目 | SillyTavern（酒馆）角色卡本地管理桌面工具（Electron 43 + Vue 3 + Vite 8） |
| 仓库 | `https://github.com/tian2418671-sys/JSKZX.git`（远端 `origin`） |
| 当前版本 | **v2.2.13**（已发布：GitHub Release + OTA 已验证） |
| 当前分支 | 本地 `master`（与 origin 同步） |
| 构建产物 | `sillytavern-card-manager-<版本>.exe` + `latest.yml` + `.exe.blockmap` + zip 绿色版 |
| 测试 | `npm test` → 504 用例全绿（44 个测试文件） |
| 大库基准 | 11,186 卡 / 9.76GB（日常库 75 张；2 万卡副本由脚本现造） |

---

## 🔒 铁律（必须遵守）

1. **没收到用户推送/打包指令，禁止推送 / 打包 / 发 Release** —— 用户会先自己看效果再决定。
2. **发布策略以用户最新指令为准**：2026-09-04 起用户已要求正常发布（已发布至 v2.2.13）；历史「仅本地不发布、不打包」限制作废，但仍须用户明示才发布。
3. 用户常报「某功能坏了」—— 先**验证代码现状**（grep / 读源码）再判断，**勿盲改**（历史上多次是误报）。
4. 用户提供的代码方案要**适配本项目架构**（Electron IPC、`app://`、路径白名单、`confirmDialog`/`nativeAlert`、Options API 组件规范）再落地。
5. 重要功能改动后必须过三关：`get_errors` → `npm run build:web` → **真实启动冒烟**（`npx electron . --disable-gpu --enable-logging`）。`vite build` 只验编译，**不验运行时**。
6. 改文件前先 grep 现状（`replace` 的 oldString 与文件不符会失败）；大段替换后立即复查。
7. **文档分工铁律**：`RELEASE_NOTES.md` 是**对外**的（只写用户能感知的变化）；文件名 / 函数名 / 脚本名 / 内存 MB 数 / 阶段耗时 / 崩溃原文一律只写进 `CHANGELOG.md` 与 `docs/**`。细则见 `docs/发布/内部信息.md` 与 `docs/发布/用户可看信息.md`。
8. **发现新缺陷时**：先在 `docs/bugs/` 记一条（编号 + 现象/根因/修复/验证），再写修复代码。
9. **测试一律上真实库，禁止「隔离 / 模拟」糊弄** —— **读操作**（扫描 / 搜索 / 索引 / 渲染 / 统计）必须跑真实库；
   **写操作**（改标签 / 删卡 / 保存 / 移动）仍用隔离库，防误删真数据。**禁止**用造的假卡 / 空库 / 小样本
   替代真实库来「证明功能正常」—— 历史多次教训（大库闸门、索引规模、字段口径）都是**只在真库才暴露**。
   > 📌 真实库：角色卡 `E:\AI\酒馆工具\角色卡`（89 张）、压测大库 `I:\03\角色色卡`（11,849 张）、
   > 世界书 `H:\01\全局世界书`（41 本，含 6 本 ≥6MB）。
   > 📌 **配置仍隔离**（`--user-data-dir=%TEMP%\xxx`）—— 只隔离配置/缓存，**不隔离卡库**，
   > 避免污染用户的收藏夹 / 分组 / 标签。详见 `AI交接指导.md` 铁律 9。
10. 🧩 **扩展缺了自己装，不许降级糊弄** —— 需要某扩展能力（代码检查 / 文档导出 / 测试 / 预览 / 语言服务 / API 测试）
    而**本机没装、装不全、或换了台机器**时，**AI 自行下载安装后再用它把任务做完**：
    查已装 `code --list-extensions --show-versions` → 安装 `code --install-extension <publisher.id>`
    （更新须带 `@<版本>`）→ 装完新开终端，需界面命令 / 语言服务生效时提示重载窗口。
    **不得**因「本机没装」就跳过检查 / 手工糊弄 / 声称做不到；装不上时走纯 CLI 等价通道（如 `npx --yes <包名>`）兜底并说明原因。

---

## 🗂️ 文档地图

| 位置 | 内容 | 可见性 |
|------|------|--------|
| `AI交接指导.md` | 纯指导文档（架构 / 工作流 / 铁律 / 缺陷地图） | 内部 |
| `docs/bugs/**` | BUG 记录（5 个领域 + 总索引 + 历史修复速览） | 内部 |
| `docs/技术支持/**` | 代码片段 / 实测技术数据 / API 参考 / 格式说明 / 脚本速查 | 内部 |
| `docs/发布/**` | 一条龙发布流程、内部信息规范、用户可看信息规范、OTA 常见问题 | 混合（最后一份面向用户） |
| `docs/规格与计划/**` | 实现规格、移植检查报告、升级计划与排期 | 内部 |
| `docs/文档整理规则.md` | **文档整理规则**（唯一归属 / 活数字口径 / 版本号联动 / 整理 SOP） | 内部 |
| `CHANGELOG.md` | 版本技术总表 | 内部 |
| `RELEASE_NOTES.md` | 对外发行说明 | **对外** |
| `README.md` | 功能、截图、下载、构建说明 | 对外/开发者 |

---

*本文件为引导入口，详情以 `AI交接指导.md` 与 `docs/` 为准。*
