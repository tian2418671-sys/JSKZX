## SillyTavern 角色卡管理器 v2.1.3

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### 🔧 v2.1.3 更新 —— 标签持久化 / 向量模型 / 漏斗协同 / UI 性能修复

#### 🐛 标签持久化彻底修复（重启不丢失）
- **子文件夹卡标签重启丢失修复**：位于分组文件夹（剧情卡/科幻/恋爱/修仙 等）的卡片，AI 打标/手动标签在重启后全部消失——加载恢复逻辑的 `subFolder` 分支直接 `return` 跳过了覆盖层恢复。已修复：物理文件夹只管分类，标签仍按覆盖层恢复
- **分组重命名标签丢失修复**：重命名分组（= 重命名物理文件夹）后子文件夹卡标签丢失——覆盖层 key 未随路径迁移。已修复：重命名时批量迁移该分组下所有卡的覆盖层 key
- **落盘加固**：物理写盘失败 / AI 打标完成时强制立即落盘覆盖层，不再依赖 500ms 防抖 + beforeunload（极端情况可能来不及）

#### 🏷️ 「导入时忽略卡片自带标签」开关全链路修复
- 显示层 / 搜索层 / 索引层 4 处绕过点全部接入开关（此前开关只在导入路径生效，界面/搜索仍显示被忽略的杂乱标签）

#### 🧠 本地向量模型有效化
- 相似度阈值 **0.65 → 0.35**（长文 vs 短标签 0.65 命中率≈0%，实测 0.35 命中率 80% 且误报可控）
- **标签自动展开**为描述句再嵌入（「这是一个关于X的故事」），强相关标签真实可命中

#### 🔄 三层漏斗协同（规则 + 向量 + LLM）
- 规则命中后不再跳过向量层：**规则精确命中 + 向量从候选池语义补充**，两者配合使用，都未命中才交 LLM 兜底

#### ⚡ UI 性能
- 字号滑块卡顿修复：草稿值 + 松手提交，拖动不再每帧触发全页面重排/写盘

#### 🧪 测试基建
- `npm test` 限定 `test/` 目录（修复 Electron 热测试脚本被误收集）
- 更新过时断言以匹配覆盖层恢复新行为（50/50 测试通过）

---

## SillyTavern 角色卡管理器 v2.1.1

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### 🔧 v2.1.1 更新 —— 换组/标签/列表刷新修复

#### 🐛 换组全面修复
- **右键换组 / 批量移动**改为**从已有分组列表选择**（可新建）：修复手输名称与物理文件夹不一致导致换组失败
- **编辑器分组下拉回滚修复**：修复选择分组后自动弹回原分组（shallowRef 缓存旧值导致移错组）

#### 🐛 列表刷新修复（同类 shallowRef 问题 3 处）
- **重命名**后列表卡片名即时刷新
- **换卡图**后列表头像/文件名即时刷新
- **保存**后「修改时间/大小」排序即时生效

#### 🏷️ 标签一致性修复
- **编辑器标签区**与侧边栏列表数据源统一（合并原生 `data.tags`），修复"编辑器无标签、列表正常"（约半数卡片受影响）
- AI 打标 / 手动 / 批量 / 全局标签全部路径已核实双写一致

#### 🎨 侧边栏标签增强
- 新增「🏷️ 标签」显示开关（可关掉节省空间）
- 列表项「+N」展开显示全部标签
- 选中态标签高对比配色（深蓝底白字，解决蓝色选中背景看不清字体）

#### 🚀 性能
- 重命名列表刷新 150ms 防抖，万卡库输入不卡顿

---

## SillyTavern 角色卡管理器 v2.1.0

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v2.1.0 更新 —— 可配置规则 + 智能查重 + 万卡性能优化

#### 🎛️ 自动打标规则可配置化
- **38 条系统预设规则内置**（世界观/题材、种族/物种、人物类型、性格/关系 4 组），开箱即用、默认全部生效
- **自定义规则**：支持添加/编辑/删除自己的正则规则（名称 + 正则，实时生效）
- **自定义关键词库**：单独维护关键词列表，配合规则使用
- 规则表入口：AI 打标面板向量引擎区「📝 管理规则表」，配置自动落盘重启不丢

#### � 小型本地向量引擎（免费 · 离线 · 不消耗 Token）
- **本地语义匹配**：内置多语言向量模型（MiniLM-L12，支持中文，约 113MB），卡片与候选标签做余弦相似度匹配，规则没命中但语义相似的卡自动打标
- **完全离线**：模型本地推理，不调任何 API、不消耗 Token；下载失败自动三源切换（国内镜像 → 官方 → GitHub 仓库分片兜底 + 加速代理）
- **智能缓存**：标签向量索引落盘持久化，重启不重算；相似度阈值 / 每卡匹配数可调；模型状态、下载进度、一键删缓存一目了然
- 三层漏斗：**① 规则 → ② 本地向量 → ③ LLM API**，前两层免费兜底，仅剩余卡片才消耗 API 额度

#### �🧬 智能查重全面升级
- **同名查重与版本清理**：一键识别同名/版本重复卡片并清理
- **内容级版本查重**：跨名称识别改名/复制的相似内容（与名称无关，内容指纹比对）
- **预设查重**：按预设名聚类 + 内容指纹比对，一键清理重复预设到回收站（可找回）
- 支持角色卡 / 世界书 / 预设三种视图，查重入口随视图自动切换

#### 🚀 万卡性能优化（真实 11.5GB / 1.1 万张卡实测）
- **扫描提速**：文件元数据 128 路并发 stat，万卡扫描秒级完成
- **PNG 内嵌缓存**：内嵌卡片 JSON 提取结果落盘缓存，二次启动免重读
- **CPU 多线程**：Web Worker 并行解析 + 拉取/解析流水线，解析提速约 33%
- **写盘降噪**：自动打标仅写入新增标签，避免重复重写大图
- 实测首屏：46.7s → 33.2s

#### 🐛 修复
- **中文搜索失效**：修复中文分词索引 bug（此前中文搜索会误返回全库），现在精确命中

---

## SillyTavern 角色卡管理器 v2.0.0

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v2.0.0 更新 —— 预设管理引擎 + 九种排序 + 千库扫描提速

#### ⚙️ 预设管理（全新引擎）
- **桌面版新增「⚙️ 预设」页签**：加载酒馆预设目录、名称搜索、批量导出
- **预设深度编辑器**：脚本 / 正则分区在线编辑（启用开关、条目折叠、说明字段），渲染型脚本支持**沙箱 iframe 渲染预览**（无权限环境隔离，依赖酒馆环境 API 的部分会提示）
- **完整管理操作**：重命名、复制副本、移入回收站（不物理删除，可找回）

#### 🔀 排序功能全面升级（9 种排序方式）
- **新增 7 种排序**：本地文件最新 / A-Z 倒序 / 修改时间 / 创建时间 / 大小倒序 / 大小正序 / Token，加上原有「导入最新 / A-Z 正序」共 **9 种排序方式**
- **中文拼音 + 数字自然排序**：`V2` < `V10`、`第2章` < `第10章`，不再出现「V10 排到 V2 前」
- **终极稳定排序链**（路径 → 文件名 → id）：重扫、重启、升级后相对顺序完全确定，**排序绝不乱飘**
- **导入时间持久化**：卡片首次入库时刻落盘（`app_config.json`），重启后「导入最新」保持稳定
- **Token 排序**：WeakMap 缓存 + 预计算（Schwartzian transform），千卡库切换零卡顿

#### ⚡ 搜索性能升级
- **高性能倒排索引引擎**：异步分片构建（不阻塞界面），大库全文检索秒回

#### 🚀 扫描性能大幅提速
- **世界书 / 预设扫描**：增量缓存（mtime 未变跳过解析）+ 32 路并发 JSON 解析 + 大文件头 64KB 预检 + 深度限制 + 黑名单目录剪枝，二次启动从数秒降至毫秒级
- **PNG 内嵌卡片 JSON 并发提取**：64 路批量提取，千卡库扫描提速一个数量级

#### 📱 安卓版预告
- **安卓版正在编写中**，未来将发布测试版，敬请期待！

---

## SillyTavern 角色卡管理器 v1.9.0

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.9.0 更新 —— 推送目标管理 + 顶部推送菜单 + 未勾选回退当前卡

#### 🚀 多推送目标管理（新功能）
- **不再只推酒馆**：可添加任意自定义卡库目录（TT 酒馆、云盘同步目录等）作为推送目标，互不冲突
- **双模式切换**：🍻 SillyTavern 酒馆 / 🗂️ 自定义卡库，一键切换当前推送去向
- **多目标列表**：支持新增（弹窗选目录 + 命名）、下拉切换、重命名、删除（仅移除快捷目标，不删真实文件夹）
- **本地物理拷贝**：直接复制卡片 PNG/JSON 到目标目录，目标存在同名卡时先备份进回收站再覆盖，绝不物理删除原件
- **兼容任意目录**：目标目录须经系统弹窗显式授权（防恶意脚本越权写盘）

#### 📋 顶部推送菜单 + 推送对话框
- 顶部菜单栏新增「🚀 推送(P)」菜单：推送选中卡片 / 当前目标预览 / 新增卡库目标
- 点击弹出**推送对话框**（PushModal）：目标模式切换、卡库目标管理、一键推送，一目了然
- 侧边栏推送面板移除、实验菜单清理重复入口，界面更清爽

#### 🃏 推送逻辑优化
- **未勾选卡片时自动回退推送「当前打开的卡片」**——打开即推，无需先勾选
- 右键单卡「🚀 推送到当前目标」直接推送该卡
- 推送完成提示覆盖/回收站明细；目标授权失效时引导重新选择并自动重试

#### 🛡️ 安全加固
- `library:pushToFolder` 白名单自扩权后门修复（对齐 v1.8.5 指纹验证模式）：目标目录必须已在白名单（本会话经弹窗真实选择）才放行，杜绝渲染层脚本注入后把任意目录塞进白名单

## SillyTavern 角色卡管理器 v1.8.9

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.9 更新 —— 世界书导入导出 + 状态栏渲染预览 + 词条字段映射修复

#### 📊 状态栏渲染预览器（新功能）
- **所见即所得调试**：右侧编辑器新增「📊 状态栏」Tab——把 AI 输出的 `<status>...</status>` 文本块应用卡内「渲染型」正则脚本（替换串含 HTML 标签的脚本），实时预览酒馆聊天中渲染出的 HTML 状态栏效果
- **一键注入内置模板**：无渲染脚本的卡片可一键注入「状态栏渲染 (Status Panel)」正则模板（V2/V3 双写字段兼容），AI 按 `<status>` 格式输出时自动渲染为深色状态面板；重复注入自动拦截
- **📚 15 套渲染模板库**：dark-rpg / cyber-hud / 江湖 / cozy / ghostly / mini / pixel / relation / log / card / wave / mind / star / lord / theme——每套内置图标/配色/动画，点击卡片即注入对应正则脚本（替换现有模板时自动拦截确认）
- **📜 11 套世界书指令模板（三合一）**：通用三合一 ⭐ / 奇幻冒险者 / 克苏鲁调查员 / 赛博朋克雇佣兵 / 江湖武侠 / 星际舰长 / 黑暗奇幻 / 现代日常 / 领主经营 / 怪物异种 / 时间旅行者——每套均为「初始值定义 + 状态栏显示格式 + 数值更新规则」三合一条目，点击即在角色卡内嵌世界书注入常驻条目（keys 留空 / constant / 插入深度 0），指导 AI 按规则输出状态文本，与渲染模板配合使用
- **模板库折叠收起**：渲染模板库与世界书指令模板库各自带折叠按钮，收起后仅保留标题行，节省编辑器空间
- **脚本勾选隔离**：自动列出卡内全部渲染型脚本，可勾选/取消单个脚本参与预览，隔离定位样式问题
- **双视图 + 安全清洗**：✨ 渲染效果 / 📄 替换后源码 双模式切换；渲染结果经 DOMPurify 白名单清洗（禁脚本/事件/外联，防追踪像素与内网探测）
- 正则引擎模拟：兼容 `/pattern/flags` 与裸 pattern，强制全局替换（与酒馆一致），`$1` 捕获组原生支持

#### 📥 世界书导入导出（新功能）
- **从世界书库导入词条到角色卡**：角色卡内嵌世界书编辑区新增「📥 从世界书库导入」按钮（空状态也有入口），复用 WbImportModal 弹窗——选源世界书 → 勾选词条 → 字段转换（库 `key/keysecondary/order` → 内嵌 `keys/secondary_keys/insertion_order`）→ 追加到内嵌 `character_book.entries`
- 与已有「📤 提取为世界书」功能形成双向闭环：卡 → 书、书 → 卡 全链路打通
- 深拷贝清洗 `_` 前缀临时字段与 uid，不污染卡片 JSON（与 `wb:create` 同一清洗口径）

#### 🐛 BUG 修复
- **世界书库条目名/触发词显示混乱**：世界书库 IDE 左侧列表原以触发词作主标题、名字（comment）为空时不显示 → 视觉上「触发词被当作名字」；改为**名字主显示**（`comment || name || '未命名词条'`）+ 🔑 触发词小字副显示，与角色卡内嵌世界书风格统一（列表 + 导入弹窗双修）
- **名字输入框标签误导**：世界书库词条名称输入框标签改为「📝 名称 / 备注 (Comment)」（原仅标「备注」，用户误以为名字不可改），与角色卡内嵌世界书一致
- **世界书导出条目名缺失**：V1 旧卡 / RisuAI 等第三方卡词条用 `name` 存名字，导出到世界书库后库 IDE 只读 `comment` 导致名字「消失」——补 `name → comment` 映射（与 JSONL 导入/全库词条搜索同口径）
- **权重回退映射**：纯 V2 卡词条只有 `insertion_order`，导出后库 IDE 的 `order` 权重字段为空——补 `insertion_order → order` 回退

## SillyTavern 角色卡管理器 v1.8.8

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.8 更新 —— 架构重构 + 新版本浮标 + 世界书快照增强

#### 🃏 架构重构：App.vue 三大业务域拆分为组合式函数
- 📦 **`useCardCrud`**（卡片 CRUD 域，-460 行）：导入入库（parseAndAddCard 影分身双重去重 / processElectronFiles staging 分块 / 拖拽 / URL 导入）、删除回收、持久化保存、自动分类打标（覆盖层优先级链）、导出重命名
- 🛡️ **`useConfigPersistence`**（统一配置持久化中枢）：`syncConfigToDisk` 收集 21 个 ref → API Key 加密 → 原子落盘；500ms 防抖合并写放大；`isRestoringConfig` ref 化启动恢复闸门；关闭前冲刷
- 🌍 **`useEmbeddedWorldbook`**（角色卡内嵌世界书）：词条派生（V1/V2 + 脏形态安全提取）、WeakMap 稳定 uid、折叠展开、触发词编辑
- ✅ 函数体逐字迁移（含 v1.8.5 全部修复注释），子组件零改动，行为零变化

#### 🔔 新版本角落提醒浮标（不打断操作）
- 静默检测到新版 → 右下角绿色浮标点亮（`🚀 发现新版本 vX.Y.Z`），**不再强制弹大弹窗**
- 点击浮标打开原有更新详情窗（日志/下载进度/重启安装）；点 ✕ 本次启动内不再提醒
- 手动「检查应用更新」保持原行为（直接弹窗）

#### 📸 世界书快照增强
- 🔄 **修复「回滚快照无限增长」**：统一备份函数三重防护——内容哈希去重（已留档跳过）+ 超量自动清理（可配置）+ 备份失败中止防覆盖丢失
- 🗑️ **快照单条删除**：快照弹窗每行新增 🗑️ 按钮（双保险路径/格式校验，防任意文件删除原语）

#### 🧪 质量保障
- ✅ **新增 13 个优先级链单测**（`test/cardCrud.test.mjs`）：锁定「物理文件夹 > 覆盖层 > 导入历史 > localStorage > 自动规则」恢复链，防重扫冲刷用户配置回归
- ✅ **46 个单测全绿** + vite build 通过 + 真实启动冒烟（60 卡加载 / 内嵌世界书编辑 / 配置落盘）

## SillyTavern 角色卡管理器 v1.8.5

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.5 更新 —— 千卡库性能大修（启动缓慢/崩溃/白屏/未响应）+ 安全加固 + 全面 BUG 修复

#### 🚀 性能优化（针对「上千卡片启动缓慢、崩溃、白屏、未响应」）
- ⚡ **主进程扫描异步化**：库目录递归扫描改为异步分片（每 25 项让出事件循环），千卡库扫描不再阻塞主进程 → 不再「未响应」
- 🌊 **自动打标 I/O 风暴治理**：启动加载期只收集变更、加载完成后低并发后台落盘——旧版启动 = 千张卡 × (整 PNG 读回 + 重写 + 快照备份) 的 I/O 风暴直接把启动拖到分钟级，现 UI 秒开
- 📦 **卡片分块加载**：批量导入/加载用暂存数组解析，每 500 张分块合并库列表，避免逐卡触发全列表计算属性重算
- 🧮 **Token 估算缓存**：WeakMap 按卡缓存估算结果，侧栏徽章/排序不再重复全量计算
- 🔢 **tokens 排序预计算**：Schwartzian transform 预计算后排序，杜绝 O(N log N) 次重复 Token 估算

#### 🛡️ 安全加固
- 🔐 **wb:scan 白名单自扩权后门修复**：旧版对传入目录无条件加入读写白名单（被注入的渲染层脚本可传任意目录一键穿透整个安全模型）；现必须通过「世界书指纹验证」（目录内存在有效世界书文件）或本会话真实选择才授权
- 🔐 **wb:restoreSnapshot 路径越界封堵**：快照路径必须位于 userData 备份目录内——旧版可传任意本地文件（如 ~/.ssh/id_rsa）构成「任意本地文件读取」原语
- 🌀 **符号链接/junction 环路防护**：4 处递归扫描（库扫描/世界书扫描/备份清理/全盘扫描）加入 realpath 去重，指回祖先的链接不再造成无限递归、内存耗尽与栈溢出

#### 🐛 BUG 修复
- 💾 **盘符根目录库全库 403**：库根 = 盘符根（如 `D:\`）时路径包含判定失效，缩略图/读写/删除全部被拒——改用 `isPathUnder()` 统一判定
- 💾 **配置损坏误迁移**：`app_config.json` 存在但损坏时旧版落入「首次使用迁移」分支，用旧配置子集静默覆盖（API Key/覆盖层/UI 状态全丢）；现损坏时保留原文件、以默认值运行
- 🔄 **刷新死循环**：保存成功后回传新 mtime 并回写内存 `_mtime`——否则每次「刷新库」按 mtime 差分把已保存卡误判为已变化，重新解析再次触发自动打标写盘 → mtime 又变 → 全量重写死循环
- ✏️ **切库后孤儿编辑面板**：切库后按路径重绑正在编辑的卡片（同卡在新库则重开，不在则关闭编辑器），Ctrl+S 不再保存失败
- 🗑️ **回收站同名互覆**：批量回收不同子目录同名卡时 `时间戳_序号_文件名` 命名，不再互相覆盖丢文件
- 🖼️ **PNG 多块救援**：含多个 chara/ccv3 块的卡片首块损坏时继续扫后续块，不再整卡放弃
- 💿 **原子写入全覆盖**：saveCard / 世界书保存 / PNG 升级路径统一 tmp 唯一命名 + rename，写入中断不再产出半截损坏文件、并发保存不再互踩；webp/json 升级 PNG 前补快照备份
- 📶 **IPC 异常兜底**：file:readBuffer / readText 补 try/catch，文件被外部删除/独占时不再以 unhandled rejection 逃逸
- 📥 **下载体积前置校验**：URL 导入/下载按 Content-Length 在响应头阶段拒绝超大文件，不再先吃满内存才报错
- 💬 **多条 system 提示词拼接保留**：主提示词 + 越狱设定分开传时不再只留最后一条
- 🩹 网络下载中断的虚假「崩溃」日志抑制；开发端口占用顺延（5174+）后页面导航误拦截修复
- 🌍 **世界书字典形态 entries 补齐**：JSONL/JSON 导入与全库词条搜索补上字典形态 `{ "0": {...} }` 处理（v1.8.4 遗漏的两处消费点）

#### 🧪 质量保障
- ✅ 33 个单测全绿（含脏卡形态回归锁定）
- ✅ main.js / preload.js / 全部改动 composable 语法校验通过
- ✅ extractBookEntries 全形态（数组/字典/数组 book/函数 entries/null）运行时验证通过

## SillyTavern 角色卡管理器 v1.8.4

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.4 更新 —— 修复「导入 JSON 角色卡导致角色栏消失/空屏崩溃」（character_book 脏形态全链路防御）

#### 🐛 核心修复：导入 JSON 角色卡后角色栏消失 / 空屏 / 重启后依旧崩溃
- 🧭 **根因**：部分 JSON 角色卡的 `character_book`（内嵌世界书）为特殊形态——① `entries` 为字典对象（SillyTavern 世界书导出的标准形态 `{ "0": {...} }`）；② `character_book` 本身是数组（老 V1 嵌入格式，此时 `.entries` 命中数组原型方法而非条目集合）。旧写法 `book.entries || (Array.isArray(book) ? book : [])` 在这两种形态下分别拿到「字典对象」和「函数」，后续 `.forEach` / `JSON.stringify().toLowerCase()` 直接 TypeError
- 💥 **症状链**：该函数被侧栏列表每张卡的渲染（Token 徽章）调用 → 渲染抛错中断 → 角色栏整体消失/空屏；卡已复制入库 → 每次重启重新加载都崩（与历史 bug `bdced8a`「侧边栏消失」同源，本次为同类陷阱复发）
- 🛡️ **修复方案**：新增 `extractBookEntries()` 统一安全提取（数组优先识别避开原型方法陷阱 → entries 数组 → 字典 `Object.values` 转数组；脏条目过滤；任何形态永不抛错），全链路接入 9 处消费点

#### 🔧 修复范围（9 处）
- 侧栏 Token 徽章估算 / tokens 排序（崩溃主入口）
- 世界书词条编辑器面板（`worldbookEntries`）
- 编辑器 Token 统计（`cardTokenStats`）
- 全局资产聚合 / 全库词条搜索（字典形态世界书此前无法被搜索，现已可搜）
- 高级搜索 `wb:` 语法（数组形态 book 下搜索即崩）
- 「带世界书」快捷筛选与侧栏 🌍 标记（字典形态此前漏判不显示）
- 内嵌世界书提取为独立世界书（字典形态此前提示"无词条"）
- 世界书导出判定（数组形态 book 误报"没有世界书"）
- **浏览模式（无搜索词）加卡片级 try/catch 兜底**：任何未来脏卡只跳过该卡并告警，列表永不整体崩溃——结构性根治「角色栏消失」复发

#### 🧪 质量保障
- ✅ 新增 5 个回归单测锁定此陷阱（含数组原型方法陷阱用例），共 33 个单测全绿
- ✅ 20 种真实世界脏卡形态（数字/数组/对象/字典/超大数/深层 null 等）全链路复现脚本验证通过
- ✅ 生产构建验证通过

## SillyTavern 角色卡管理器 v1.8.3

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.3 更新 —— 全库词条搜索 + 世界书库重设计 + 世界书扩展 + 快照管理增强 + 关系图谱卡顿修复 + 白名单安全加固 + 全面 BUG 修复

#### 🔍 全库词条搜索（跨世界书 + 角色卡内嵌）
- 🆕 顶栏「🔍 全库词条搜索」：一次性检索**全部独立世界书 + 全部角色卡内嵌词条**的触发词 / 次级词 / 备注 / 正文
- 🎯 命中结果标注来源（所属世界书 / 所属卡片），点击直达并高亮目标词条

#### 🌍 世界书库 UI 重设计
- 🎨 侧边栏世界书库改为 4 层布局：搜索独立行 → 操作按钮等宽（合并 / 查重 / 全库 / 折叠）→ 分组导航常驻 → 3×2 统计网格卡（Token 格式化显示）
- 🧰 高级功能区：URL 导入 / 打开目录 / 落盘 / JSONL 导入 / 批量导出 / 筛选 chips

#### 📦 世界书扩展
- 🕒 **世界书快照**：世界书保存前自动备份 + 弹窗查看 / 一键恢复（同角色卡快照机制）
- 📥 **JSONL 批量导入**：逐行解析（兼容整体 JSON / {entries} / V2 data.entries / JSONL）
- 📤 **批量导出**：多本世界书一键导出到自选文件夹
- 🧩 **内嵌世界书提取**：角色卡内嵌词条（keys/secondary_keys）提取为独立世界书文件，字段自动转换（key/keysecondary）

#### 🎛️ 世界书词条 IDE 增强
- 🔍 词条筛选 / 排序 / 批量操作 / 词条体检（空内容 / 无触发词等异常标记）
- ⬆️⬇️ 词条上移 / 下移排序；Token 用量显示
- 📤 工具栏新增「提取为世界书」「🕒 快照」按钮

#### 🗑️ 历史快照管理增强
- 🆕 **删除单个快照**：快照弹窗每行新增 🗑️ 按钮，确认后删除（双重安全校验：仅限 `.bak_history` 内 + 快照文件名格式）
- 🔧 快照前缀匹配精确化（卡 A 不再误配卡 A_1）+ 快照工具函数提取 + 自动化单测（9 用例）

#### 🚀 关系图谱打开卡顿修复（v3 + v4）
- ✂️ **连线预算**：同作者 / 同分组 / 同标签成员超 300 的群体不再两两展开（🚫 徽标提示）；渲染仅保留权重最高的 **3000 条**（角色卡）/**8000→4000 条**（世界书）连线（✂️ 徽标提示裁剪数）——千卡库连线从数十万降到预算内，力导向模拟从分钟级降到亚秒级
- ⚡ **构建缓存**：打开时构建一次，之后的 过滤 / 搜索 / 阈值 / 布局切换全部走缓存毫秒级重渲
- 🌀 **loading 遮罩**：打开先出转圈遮罩再后台构建，弹窗绘制不冻结、可随时关闭
- ⏱️ **移除 `layoutAnimation:false` 回归**：恢复逐帧收敛，打开立即出图、节点逐渐落位，UI 始终可交互
- 🧩 **世界书图谱分批异步构建**：每 80 词条让出主线程一帧，大书构建全程流畅
- 🖼️ **头像节点限流（v4）**：>120 张卡时仅度数 Top60 枢纽保留头像，其余退化为圆点——不再同时解码几百张 1-3MB 立绘拖垮渲染进程
- 📏 **过绘制治理（v4）**：节点尺寸按规模分档收缩（30/18/11px + 度数加成）+ 标签预算封顶（角色卡 60 / 世界书 80，按度数排名）
- 🧭 **位置种子（v4）**：切换 过滤 / 搜索 / 阈值 / 布局 前捕获旧坐标注入，图谱保持在原位瞬时变化、不再整图重新洗牌收敛数秒
- ⚡ **bigram 预过滤（v4 世界书）**：key 命中从全文 `includes` 降为 O(1) Set 查询，大书构建提速 10-100 倍
- 🔧 修复「📷 导出」按钮失效与统计徽标空白（`exportGraph`/`graphStats` 漏暴露）；修复 loading 遮罩不显示（`graphBuilding` 漏暴露）
- 🎯 **入口统一**：顶栏「关系图谱」按钮按模式智能分流（角色卡 🌌 / 世界书 🌍），删除「实验与工具」菜单重复项；世界书 IDE 保留「🌐 关系图谱」上下文快捷键

#### 🔒 白名单安全加固
- 🛡️ **换卡图不再误删库根授权**：新图选自库目录内（高频操作）时不再回收本已存在的白名单——此前会撤销整个库根导致保存 / 删除 / 缩略图 / 刷新全「路径越界」大面积失效（重启才恢复）
- 🛡️ **盘符扫描同类修复**：卡片库直接放在盘根的用户全盘扫描后不再被撤销盘根授权

#### �️ 全盘打捞 V3：真伪鉴定引擎（不再捞壁纸/截图/贴图）
- 🔬 **黄金标准鉴定**：PNG 必须内嵌合法 chara/ccv3 数据块（块级解析 + JSON 可解码 + 有角色名）；JSON 必须是角色卡结构（世界书剔除）；WebP 深扫 base64 卡数据段
- ⚡ **IO 优化**：先读头 / 尾 64KB 做特征预筛（chara/ccv3/eyJ），命中才完整读入精确解析——普通壁纸只付出 128KB IO 即被剔除，全盘扫描不拖慢
- 🔄 **库内排除（V3.1）**：当前库目录树内的卡 + 与库内同名的副本（含酒馆推送拷贝 / 备份副本）自动跳过——结果页只显示真正值得收编的「库外新卡」
- 📥 **无库收编引导（V3.1）**：没开角色库点收编 → 弹确认引导选库 → 选完自动继续收编，一步到位（不再直接失败）
- 📊 **收编三路明细**：成功 / 同名跳过（含文件名）/ 失败分项反馈；单文件失败不中断整批
- 🧵 **批量收编异步化**：全程 `fs.promises`，几千张卡复制不再冻结主进程
- 🔧 **修复收编「导入失败: An object could not be cloned」**：`foundFiles` 是 Vue 响应式数组，直接传 IPC 无法 structured clone——先 JSON 剥离再传

#### �🛠️ 稳定性与 BUG 修复（审查整改）
- 🧹 批量操作 / 查重清理接入**进度条**（`runWithProgress` 进度执行器 + 并发安全 Toast 工厂）
- 🗑️ 删除卡片后自动清理残留覆盖层（三个删除入口统一接入 `deleteCardOverlays`）
- 🏷️ 标签系统「只增不减」修复：按差集精确移除，保存不再复活已删标签
- 💾 卡片数据规范化纯函数化（`normalizeCardData`/`calibrateCardData` 深拷贝，不再原地污染）
- 🔁 目录扫描环路防护（realpath 去重 Set）
- 📥 导入同名文件正确跳过（`skippedExisting` 计数，不再中断整批）
- 🧹 修复 L71 孤立 `/**` 注释吞掉快照工具函数事故；快照文件名正则升级为带缓存版本
- 📤 回收站部分失败分项报告（成功数 / 失败列表），前端按失败路径精确过滤，无「幽灵卡」残留
- 🔧 推送酒馆覆盖前自动备份旧版到全局回收站（可找回）
- 🌐 协议 / 白名单越权路径加固；PNG 保存改为 `tmp + rename` 原子写；快照失败即中止保存防止半写

## SillyTavern 角色卡管理器 v1.8.2

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.2 更新 —— 换卡图 + 链接下载导入 + 工具栏下拉菜单 + 静默升级 + 安全加固 + 多项 BUG 修复

#### 🖼️ 换卡图
- 🆕 工具栏 ⚙ 菜单 / 右键菜单新增「🖼️ 换卡图」：选择新立绘一键替换当前卡片
- 📦 PNG 卡**原地替换**（内嵌角色数据完整保留）；WebP / JSON 卡自动转为标准 PNG 卡
- ✅ 替换后自动结构校验 + 数据校准再落盘，绝不出废卡

#### 🌐 从链接直接下载导入角色卡
- 🆕 顶部工具栏「🌐 链接导入」+ 文件菜单「从链接导入角色卡...」
- 🔗 粘贴 URL 直链（Discord / GitHub 等 CDN 均可），自动下载并识别角色卡
- 🎴 支持 PNG 卡（内嵌 chara/ccv3 块）与 JSON 卡；以卡片名自动命名、同名跳过不覆盖
- 🛡️ 非角色卡文件 / 超限文件明确报错，不污染卡片库
- ⏳ 进度改为非阻塞提示，完成后弹出导入结果

#### ⚙️ 编辑器工具栏下拉菜单
- 🎛️ 7 个操作按钮（汉化 / 升维 / 快照 / 换卡图 / 保存 / 导出 / 删除）收进 ⚙ 下拉菜单
- 🧭 菜单悬浮渲染于最顶层，彻底解决展开遮挡 / 裁剪问题；点击菜单项执行并自动关闭
#### 🔄 更新后静默升级（不再弹「重装向导」）
- 🆕 更新包下载完成后点击安装 → **静默覆盖旧版、不弹安装向导界面，装完自动重启**，全程无需手动操作
- 📂 首次安装依旧可**自选安装目录**（不装 C 盘，可装 D / E 盘等），更新自动沿用原目录
- ✅ 保持 per-user 安装（免管理员权限），静默更新才能成功写入
#### 🐛 Bug 修复
- 📁 修复卡片导入「空分组」问题
- ↔️ 修复编辑器内容区右侧大面积空白（解除内容最大宽度限制，内容随窗口铺满）
- 💾 修复历史快照配置重启后丢失（快照开关 / 冷却 / 保留数持久化）
- 🗂️ **修复「最新」排序错乱**：改以物理**创建时间 birthtime** 为第一基准——实测全库 mtime 会被批量 touch 统一成同一时刻，导致排序失效；birthtime 稳定反映「入库时刻」，新导入卡片正确排最前
- 📸 **修复「关闭自动快照仍生成快照」**：快照开关经 IPC 同步主进程时因 Vue reactive Proxy 无法被 structured clone 而静默失败，主进程始终默认开启——现改为序列化剥离后同步，关闭开关立即生效
- 💬 修复保存成功弹窗报错（`showMessage` 收到非标准 type `success` 抛 `Invalid message box type`），自动归一为 info
- 🗂️ **修复「导入卡片出现无名 / 陌生分组」**：自动分类不再把命中规则名（如 `Monster (魔物娘)` → `Monster`）当作新分组创建——分类只落到已知预设分组（奇幻 / 科幻 / 限制级 / 恋爱），其余保持「未分类」，不再冒出莫名英文分组
- 📁 **修复空物理文件夹显示为空分组**：扫描时只把「确实包含卡片文件」的文件夹作为物理分组，误建 / 残留的空文件夹不再显示为分组

## SillyTavern 角色卡管理器 v1.8.1

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.1 更新 —— 快照空间治理 + 配置持久化彻底修复 + AI 打标增强

#### 📸 快照空间治理（.bak_history 不再膨胀）
- 🧹 **一键清理全部历史快照**：设置面板新增「一键清理全部历史快照」，递归删除库目录所有 `.bak_history`（含物理分组子文件夹），并报告释放空间
- 🔍 **内容哈希去重**：自动快照时对比最新一份快照的 SHA-256，内容未变化直接跳过——重复保存不再堆积重复备份
- 🗑️ **库级孤儿快照清理**：卡片被删除后其 `.bak_history` 不再永久残留；一键清理「孤儿快照」只删无对应卡片的目录，有卡片存活的快照安全保留
- ⚠️ 孤儿判定采用正向匹配（卡片名为快照前缀），卡片名含下划线时也不会误删

#### 🔒 配置持久化彻底修复（重启/重扫不再丢失或复活）
- 🏷️ **删除标签/清空不再复活**：`app_config.json` 成为唯一权威，恢复改【整体替换】+ 装载标记（`isRestoringConfig`）防竞态自污染；删除的默认标签、清空的标签池重启后保持原样
- 📁 **分组删除/清空正确落盘**：自定义分组空数组（全部删除）也能覆盖保存
- ⚙️ **API 清空正确落盘**：Endpoint/Model 清空后重启保持为空
- 🧩 **12 个 UI 字段统一收口**：主题/字号/导入清洗开关/快照配置/卡片分类映射/侧栏宽度/视图模式/紧凑模式/排序/系统提示词预设/世界书目录/世界书分组——生产版（app://）下 localStorage 不持久，现全部迁入 `app_config.json`，重启不丢失
- 🗂️ 移除旧文件双写（`tavern_manager_config.json`）与双源竞态，单一权威落盘

#### 🤖 AI 智能打标增强
- 🚨 **破限（Jailbreak）机制**：对抗傲娇/病娇/反派/NSFW 卡打标时模型拒答与道德审查；破限词拼接到系统提示词最末尾（注意力权重最高），自动保存重启不丢
- 📚 **破限词预设库**：3 套不同强度/场景预设（打标专用·默认 / 强硬指令 / 学术测试），一键套用
- ⚡ **限流 + 429 退避重试**：卡片间 1.5s 节流，429/网络抖动自动重试（2s→4s→8s 指数退避），400/401/403/404 业务错误直接判失败——批量打标不再大面积崩

## SillyTavern 角色卡管理器 v1.8.0

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.8.0 更新 —— 快照一键恢复 + 世界书选项卡增强 + 扫描性能优化 + 全面 BUG 修复

#### 📸 历史快照「一键恢复」
- 🆕 右键卡片 → 「📸 历史快照 / 一键恢复」：列出该卡 `.bak_history` 内的全部历史备份（时间/大小/手动标记）
- ↩️ **一键恢复**：把任意历史版本复制覆盖回原卡片；恢复前自动把当前版本备份为新快照，随时可再回退
- 📂 弹窗内可直接打开快照文件夹

#### 🎛️ 角色卡内嵌世界书选项卡全面增强
- 🔍 词条关键字搜索（触发词/次级词/备注/正文全字段）+ 筛选计数
- ➕ 新增词条（空卡片也可「+ 立即新增一条」）/ 🗑 删除（确认防误删）/ ⧉ 克隆 / ↑↓ 排序
- ✅ 启用 / 常驻显示 / 条件触发 开关 + 插入位置（顶部/底部/聊天前/@D 深度提示）
- 🏷️ 触发词 & 次级词**标签化输入**：回车/逗号添加、× 一键移除
- ⚙️ 优先级 / 权重独立可调；名称与备注字段兼容旧卡

#### 🚀 扫描性能大幅提升（千卡库加载更快）
- 🔬 主进程扫描时**本地提取** PNG 内嵌卡片数据（只读文件头 1MB），不再把整张图跨进程搬运回渲染端
- 💾 千卡库从「几 GB 跨进程传输」降至「几百 KB JSON」，启动/刷新明显提速；异常卡自动回退兼容解析，绝不漏卡

#### 🐛 全面 BUG 修复
- 🔀 **「最新」排序修复**：改以物理文件时间（修改/创建取较新）为准，不再被卡片作者创作日期（`create_date` 可多年不变/同批相同）干扰；新导入卡片正确排最前
- 🏷️ **标签池开关生效**：「导入时忽略卡片自带标签」开启后，全局标签池不再透出他人卡片的原生标签
- 📁 **不再自动建"幽灵分组"**：导入卡片时自动分类不再盲目创建分组（联动导入清洗开关）
- 🖼️ **webp 卡保存明确提示**：不再笼统报「不支持的文件格式」，明确提示仅支持 .json/.png
- ⏱️ **异步竞态修复**：AI 汉化/格式升维/聊天在途请求期间切换卡片，结果不再回写到旧卡
- 🧩 **iTXt 卡片解析修复**：标准 iTXt 数据块（含语言标签/译名段）的角色卡可正确解析
- 🔄 **双协议 max_tokens 口径统一**：Anthropic/OpenAI 一致尊重前端设置，不再单边硬编码截断
- 🪄 **渲染入口更名**：`js/main.js` → `js/entry.js`，消除与主进程 `main.js` 的同名混淆
- 📄 世界书切换后右侧不再残留旧书选中词条

## SillyTavern 角色卡管理器 v1.7.1

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.7.1 更新 —— 超级搜索引擎 + 批量悬浮框可拖动 + 稳定性修复

#### 🔍 超级搜索引擎（核心新功能）
- 🧠 **全字段穿透检索**：覆盖 100% 搜索盲区——角色名/作者/描述/性格/场景/首条开场白/对话示例/作者备注、**备选开场白列表、深度提示词、系统提示词、正则脚本**、内嵌**世界书全部词条**（名称/注释/触发词/正文）、**物理文件名与路径**
- 🔗 **多关键词 AND**：输入 `傲娇 女仆` 自动按空格拆分，卡片必须**同时包含**全部关键词才会显示
- 🎯 **高级前缀语法**：
  - `tag:奇幻` / `t:奇幻` —— 仅搜标签
  - `author:白桃` / `a:白桃` —— 仅搜作者/创作者
  - `file:卡片` / `f:卡片` —— 仅搜物理文件名与路径
  - `wb:魔导书` / `w:魔导书` —— 仅搜关联世界书内容
  - `-排除词` —— 否定排除（如 `女仆 -傲娇`）
- 💡 搜索框下方新增语法提示行，快速上手

#### 🖱️ 批量操作悬浮控制台可拖动
- ⠿ **随意拖动**：按住标题栏（"已勾选 N 张卡片"区域）即可把批量操作栏拖到屏幕任意位置
- 🎯 自动限制在窗口边界内，不会拖出屏幕
- 🔄 **双击标题栏一键复位**回底部居中

#### 🛡️ 稳定性修复
- 💥 **修复分组切换侧边栏消失 / 搜索失效**：世界书词条 `name`/`comment` 为非字符串（数字/对象）时 `toLowerCase()` 崩溃导致列表渲染中断——已全链路 `String()` 强转防护
- 🛡️ 异常畸形卡片在检索时自动跳过，绝不白屏

## SillyTavern 角色卡管理器 v1.7.0

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.7.0 更新 —— 物理文件夹分组 + 可配置历史快照 + 标签批量操作 + 稳定性加固

#### 📁 物理文件夹分组系统（核心新功能）
- 🗂️ **分组 = 物理文件夹**：库目录下的一级子文件夹名自动识别为分组，把卡片放进文件夹即完成分组
- ➕ **新建分组 = 创建文件夹**：分组列表「➕」新建分组时在本地库目录物理创建子文件夹
- ✏️ **重命名分组 = 重命名文件夹**：同步迁移文件夹内所有卡片的物理路径（自动刷新全库）
- 📦 **移动卡片 = 物理移动文件**：右侧下拉改分组 / 右键快速移动 / 批量移动，全部物理移动 PNG/JSON 文件到目标分组文件夹
- 🧹 **删除卡片后自动清理空分组**：某分组最后一张卡被删除后，空分组自动从列表移除 + 空物理文件夹自动删除（只删空目录，绝不误删有数据的文件夹）
- 🛡️ 物理移动全程白名单安全校验；目标同名文件自动加时间戳后缀绝不覆盖；「未分类/全部」归入库根目录

#### 📸 可配置历史快照系统
- ⚙️ **快照设置面板**（设置菜单）：自动快照总开关 / 冷却间隔（1分钟~1小时）/ 单卡最大保留数（3~50份），修改即时生效并持久化
- 📸 **手动创建快照**：编辑器工具栏「📸 快照」按钮，绕过冷却立即备份当前卡片状态（文件名带 `_manual` 标记）
- 🧹 旧快照按修改时间自动清理超量，精确匹配卡片名，防磁盘膨胀

#### 🏷️ 标签批量管理
- 🧹 **一键清空**：标签快捷区一键清空系统标签库 + 清洗全库卡片全部标签并物理落盘
- ☑️ **批量删除**：标签区「☑️ 批量删除」模式——勾选多个标签一键批量删除（清系统库 + 洗全库卡片）
- 🚫 **导入数据清洗开关**：设置菜单可开启「导入时忽略卡片自带标签」，防止他人卡片的杂乱标签混入全局标签池

#### 🔄 日常体验优化
- 🔄 **一键刷新目录**：侧边栏顶部刷新按钮重新扫描当前库目录，放入文件夹的新卡无需重启即可读取
- 🗂️ **最新排序更准确**：卡片内建创建时间缺失时自动回退文件修改时间（复制/移动文件不再导致排序混乱）
- 🐌 **启动大幅提速**：卡片解析改为并发受限批处理（每批 8 张），几百张 PNG 秒开
- 💥 **稳定性加固**：修复全屏文本弹窗打不开；搜索/Token 统计/全局资产/世界书渲染全面空值防护（null 词条、数字标签不再白屏）

## SillyTavern 角色卡管理器 v1.6.5

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.6.5 更新 —— 统一持久化中枢 + 导入功能修复 + 全盘强行收编

- 🛡️ **统一持久化中枢（app_config.json 最高权威）**：全软件全局状态（分组/语言/全局标签池/API Key）统一收口到 `app_config.json` 物理文件（原子写入：临时文件 + rename，绝不丢数据）——生产模式下即便 localStorage 不持久也不丢配置
- 🎴 **卡片覆盖层防冲刷（核心）**：手动改过的卡片分组/标签写入物理覆盖层（key=卡片路径），重新扫描/重启后**绝不**被自动分类覆盖（实测重扫后"恋活"分组完整保留）
- 💾 **卡片变更三保险落盘**：新增 `persistCardUpdate` 统一入口（内存 + 覆盖层 + 物理重写 PNG），8 个标签/分类操作全部接入——即使 PNG 重写失败，配置库也能记住数据
- 🔑 **API 配置物理持久化**：Endpoint / Key / Model 此前只存 localStorage（生产模式重启丢失），现已写入 app_config.json，重启自动恢复
- 📥 **导入功能修复（Win10 等导入不了卡片）**：文件菜单导入改用浏览器 File API 直接读取内存内容，彻底绕过 IPC 路径白名单——从桌面/下载等任意位置导入卡片不再被拒（修复"未识别到有效的角色卡文件"）
- 🚀 **全盘扫描强行收编通道**：新增 `sys:importExternalCards` 专属接口——全盘检索出的卡片可绕过源路径白名单强行复制入库（只校验目标库；同名跳过绝不覆盖；兼容字符串/对象两种格式）
- 🔄 **旧配置自动迁移**：首次启动自动把旧 `tavern_manager_config.json` 的 globalTags/uiSettings 合并迁移到 app_config.json，历史数据零丢失

## SillyTavern 角色卡管理器 v1.6.4

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.6.4 更新 —— 修复安装版分组/语言/分类重启丢失 + OTA 自动更新 + 全盘检索 + 血统鉴定

- 🔧 **根因修复：生产模式 localStorage 不持久**——Electron 的 `app://` custom scheme 的 localStorage 不落盘，安装版每次重启所有设置（分组/语言/卡片分类等）全部回退默认；已将关键 UI 状态迁移到**主进程配置文件**（`tavern_manager_config.json` 的 `uiSettings`），实测完全退出→重启→全部恢复
- 📁 **分组删除/重命名持久化**：预设分组可删除/重命名（排除「全部」「未分类」），重启不再重新生成或「改名新分组 + 原预设」重复并存
- 🏷️ **卡片分类持久化**：分组重命名/删除/移动后分类跨重启保留（配置文件为权威载体 + localStorage 兼容）
- 🌐 **语言设置持久化**：标签语言模式（纯中文/纯英文/中英双语）重启保持上次选择
- 🚫 **特殊分组按钮隐藏**：「全部」「未分类」等系统视图不显示改名/删除按钮
- 🚀 **OTA 自动更新**：升级为 electron-updater 自动下载安装——检测到新版本后应用内一键下载（实时进度条/速度），下载完成自动重启安装，无需跳转浏览器手动下载
- 🛰️ **全盘深度检索引擎 (Beta)**：实验菜单「全盘打捞卡片」极客雷达风弹窗——自动枚举全部本地磁盘、体积过滤引擎（拦截 <40KB 废图/贴图）、穿透隐藏文件夹的 V2 并发递归扫描、实时进度心跳，扫描完成后一键「全部强行收编入库」精准追加入库（同名跳过，不清空现有库）
- 🕵️ **角色卡血统严格鉴定**：入库前指纹级校验——新增拦截伪装成卡片的**聊天记录**（messages/chat_metadata）、**独立世界书**（孤立 entries）、**UI 主题配置**（colors/user_settings），连同原有的 config.json 排他与 V1/V2/V3 规范校验，杜绝脏数据污染卡片库（实测 4 类伪装文件全拦截、含无 description 的 V1 真卡不误杀）
- 💾 **持久化真正落盘修复（关键）**：`saveUiSettingsToDisk` 此前把 Vue 响应式 Proxy 直接传给 IPC，触发 Electron `An object could not be cloned` → 分组/语言/卡片分类**从未真正写入磁盘**（静默失败）；已统一用 JSON 序列化剥离 Proxy，实测语言切换 + 卡片分类修改均能物理落盘、重启恢复

## SillyTavern 角色卡管理器 v1.6.2

本地优先、完全离线可用的角色卡管理与解析工具（Electron + Vue3 + Tailwind + ECharts）。

### ✨ v1.6.2 更新 —— 深度修复 8 项 + UI 全面瘦身

#### 🔧 底层修复（8 项）
- 📋 **克隆世界书 UID 冲突修复**：复制副本时重新生成全部词条唯一标识，杜绝 Vue 渲染错乱（Duplicate keys）
- 🔀 **世界书折叠状态错位修复**：从数组索引改为稳定唯一标识，删除/排序词条后折叠状态不再错乱
- 🔌 **API 引擎切换模型回退**：切 Claude 时自动清空 `local-model`/`gpt-*` 不兼容模型名，杜绝 HTTP 400
- 🧮 **正则作用域 0 值误判修复**：`placement: 0` 不再被误判为"默认"，补全"全局/未定义"映射
- 🖼️ **PNG 重组 IEND 兜底**：保存时确保 IEND 块收尾，杜绝残缺 PNG
- 🎴 **卡片规范化 3 项**：V2 字段缺失不再白屏（tags/alternate_greetings/extensions 兜底）；缺 spec 半残卡不再双重嵌套；Blob URL 改用本地路径协议防内存泄漏
- 🏷️ **自动打标落盘**：新卡导入时自动标签/分类立即物理保存，重启不丢失
- 🧹 **V1 判定排他**：酒馆 config.json 等标准配置文件不再被误当角色卡入库

#### 🎨 UI/UX 全面瘦身
- 🗜️ **列表双模式**：常规（大头像+描述+Token/标签三行信息）/ 紧凑（极致单行，一屏翻倍卡片）
- 📁 **排序下拉**：名称 / 最新 / Token 三种排序，偏好持久化
- 🎛️ **高级筛选折叠**：分类/快捷标签/过滤 chips 收进漏斗面板，侧边栏顶部只留搜索+漏斗
- 📌 **批量操作底部悬浮台**：多选时页面正下方弹出毛玻璃控制台，不再挤占侧边栏
- 🌍 **世界书侧边栏折叠式**：URL 导入/打开目录/分组/筛选收进 ▼ 面板，与角色卡模式同款交互
- 📖 **世界书词条紧凑化**：启用圆点可点击切换、字数/位置徽章、hover 操作、列表可整体收起
- ✅ 修复重复分类下拉（"All (全部)" 只保留一个）

#### 🖥️ 世界书编辑器重构（IDE 化布局）
- 📚 **左列表 + 右详情**：从卡片内联展开改为「左侧可收起词条列表 + 右侧详情编辑」双栏 IDE 布局
- 🎯 **竖直长条折叠按钮**：浮在栏边缘垂直居中，一键收起为窄条（📖 + 竖排词条数），点击 📖 可快速展开
- 🔍 **侧栏内搜索 + 新建**：搜索框与 ➕ 新建移入左侧栏，触发词/备注/正文全字段匹配
- 🗜️ **极致压缩列表**：`formatKeys` 展示触发词（空 key 显示「无触发词」）+ 启用圆点 + hover 复制/删除（毛玻璃背景）
- ✍️ **详情区全字段编辑**：主触发词/备注 → 次要触发词/权重 → 插入位置（0-4 五档）→ 内容 textarea 撑满 + 实时字数
- 🔗 **原生字段映射**：`key` / `keysecondary` 逗号分隔双向绑定（computed），严格遵循酒馆官方字段，绝不污染 JSON
- 🛡️ **修复底部遮挡**：底部终端控制台不再遮挡词条列表最后一个条目（动态底部留白联动）

#### 🔐 安全加固（纵深防御）
- 🛡️ **渲染模式 XSS 清洗**：引入 DOMPurify（本地依赖、离线可用），聊天渲染模式剥离脚本/事件/iframe/`javascript:` 等危险内容，禁止外联图片（防追踪像素/内网探测）
- 🧱 **主进程路径白名单**：全部文件类 IPC 统一校验「卡片库/世界书目录/酒馆根/扫描根/userData」白名单，越界读写/删除/导出一律拒绝；`local-file://` 协议越界返回 403
- 🔒 **CSP 响应头**：生产模式注入完整 Content-Security-Policy（限制内联脚本与外部连接），纵深防御兜底
- 🛑 **`openExternal` 协议白名单**：仅放行 http/https，防恶意 URL scheme 触发
- ⚡ **单文件 IO 异步化**：读图/读卡/保存全链路改 `fs.promises`，几十 MB 大图不再卡主进程
- 🐛 **修复配置覆盖 bug**：选择/扫描文件夹时改为合并写入配置，不再冲掉已保存的全局标签库
- 🔓 **堵死白名单自扩权后门**：`scan-target-folder` 直接传路径严格限定为纯盘符；`tavern:pushDir` 不再无条件扩权（需已在白名单或通过酒馆指纹验证）；`FORBID_ATTR` 改为真正生效的字符串列表

### ✨ v1.6.1 更新 —— 持久化终极修复 + 5 项深度 Bug 修复

- 💾 **标签持久化终极修复（内存与物理文件断层）**：手动贴标签 / 删除标签 / 系统标签快捷添加 / 批量贴标签 / 批量添加标签 / 手动批量添加，共 **6 个入口全部统一**为「内存 customTags + 卡片原生 data.tags 双写 + 物理落盘（saveCard）」三合一
  - 此前标签只活在内存里，重启后自定义标签全部丢失；现在关闭重开标签依然保留（已实测：添加 → 重启 → 标签仍在）
- 👻 **修复世界书合并「内存幽灵」**：合并后的新世界书之前被赋予假路径导致「原文件不存在，无法保存」，永远无法落盘；现已改为触发智能落盘分配，合并结果可正常保存
- 🥶 **修复深层扫描性能灾难**：解析 WebP / 残损 PNG 时从逐字节字符串拼接（O(N²)，5MB 大图卡死主线程）改为 `TextDecoder` 毫秒级二进制转码，大图解析不再卡顿
- 💣 **修复拖拽导入全库重载**：拖入 1 张新卡片不再清空并重扫整个卡片库，改为 O(1) 增量追加，千卡库也丝滑
- 📝 **修复查重漏判场景字段**：差异比对面板补回「🎬 当前场景 (Scenario)」，场景改动不再被漏判导致误删新版本
- ⚙️ **修复配置覆盖漏洞**：启动读取个性化设置改为默认值与存档解构合并，旧版本升级后字体/样式不再错乱

### ✨ v1.6.0 更新 —— 全量 SFC 单文件组件化重构

- 🏗️ **前端工程化大重构**：将原先 4600 行单文件应用拆分为 **22 个 Vue SFC 单文件组件**（21 子组件 + 根组件），全面采用 Vite 构建管线
  - 全部 **14 个弹窗**（AI 打标 / 批量标签 / 查重 ×2 / Diff 比对 / 大文本 / 大图 / API 设置 / 全局资产 / 图谱 ×2 / 合并 / 条目导入 / 版本更新）
  - **2 个右键菜单**（角色卡 / 世界书）
  - **3 大核心区块**（HeaderBar 顶栏 / SidebarPanel 侧边栏 / EditorPanel 编辑器），通过 provide/inject 上下文共享状态
- 🐛 **修复 AI 打标弹窗静默失效**：组件注册名大小写不匹配（`ai-tag-modal` 解析为 `AiTagModal`）导致弹窗永远打不开，现已修复
- 🎨 结构清晰、模块解耦，后续功能迭代与维护成本大幅降低
- 📦 代码库结构：`js/components/` 存放全部 UI 组件，`js/utils/` 存放工具函数

### ✨ v1.5.0 更新
- 🌐 **统一系统/常用标签库**：批量设置弹窗与 AI 打标候选池共享同一份数据源，一处维护、处处同步
- 🏷️ **超级扩充版标签池（53 个精选标签）**：6 大分类全覆盖——基础/性别、种族/物种、世界观/题材、角色属性/性格、内容分级与基调、卡片功能类型；支持 localStorage 持久化，越用越懂你
- ⚡ **批量标签弹窗增强**：已选标签芯片一键移除，常用标签点击即加（已选自动高亮）
- 🤖 **AI 智能打标规则设置**：
  - 🎯 候选标签池升级为可视化管理（点击快速添加 / 手动输入 / ✕ 移除）
  - 🔓 「允许 AI 自由提取标签」开关：开启时 AI 优先从候选池挑选、不足时自由提取；关闭时严格只能从候选池选择
  - 📝 附加自定义提示词：可注入「重点分析性格特征」等个性化指令
- 🛡️ 关闭自由提取但候选池为空时自动拦截报错，杜绝无效空跑

### ✨ v1.4.1 更新
- 🌐 **世界书网址一键导入**：在世界书库顶部粘贴 Discord / GitHub 等 `.json` 直链，云端拉取自动解析并落盘到世界书库（双通道拉取：渲染层 fetch 直连 + 主进程转发兜底，彻底绕开跨域拦截）
- ✏️ **世界书重命名**：世界书卡片一键改名，内部名称与物理文件同步更新，磁盘与内存始终一致
- 🛡️ **导入安全防护**：链接失效 / 返回非 JSON 内容时优雅拦截，弹出详细错误提示，绝不白屏
- 💾 导入的世界书自动保存到上次世界书目录（或询问选择目录），可继续在线编辑与保存

### ✨ v1.4.0 更新
- 🌐 **世界书关系图谱**：基于 ECharts 的词条触发词关联可视化，点击节点直接跳转定位词条
- 🔗 **多书一键合并**：勾选多本世界书，按 Key+Content 指纹自动去重合并，生成全新合并书
- 💾 **世界书时光机快照**：每次保存前自动备份到 `%AppData%/jsTavern_Backups/worldbooks/`（保留最近 10 份）
- ✂️ **世界书拆分导出**：按当前搜索/过滤结果拆分为子集独立导出
- 🚀 **版本更新检测系统**：启动静默探测 GitHub Releases，有新版本自动弹出更新提示，一键跳转浏览器下载（轻量 API 方案，无需 electron-updater）
- 📄 **分页系统复活**：卡片列表启用分页翻页（18 张/页），告别一次性全量渲染
- ⚡ **Token 统计实时刷新**：修复 shallowRef 下编辑描述/场景等字段时 Token 与 Raw JSON 不更新的问题
- 🐛 **深度代码审查修复（10 项）**：内存泄漏（WeakMap）、右键 AI 打标保留多选、世界书导出 uid 脏数据剔除、Markdown 代码块清洗、星系图主题色适配、查重 Token 算法口径对齐、拖拽导入覆盖世界书模式、死代码清理等
- 🖼️ 修复查重弹窗"综合最优推荐"徽章被滚动容器裁剪遮挡

### ✨ v1.3.0 更新
- 🚀 **一键推送至酒馆（本地物理拷贝）**：直接复制卡片到酒馆 characters 目录，无需 API / 无 CORS / 无 403；自动兼容新版（data/default-user/characters）与老版（public/characters）结构，同名覆盖更新
- 🧭 **酒馆路径智能嗅探**：首次推送自动遍历常见位置（桌面/下载/文档/C/D/E 盘），通过 server.js + public 指纹验证并一键确认绑定；绑定后永久免密推送
- 📁 **设置面板酒馆目录管理**：随时查看当前绑定路径、一键重新选择、解除绑定
- 🐛 推送失败自动重置绑定并给出明确提示

### ✨ v1.2.0 更新
- 🔄 配置持久化彻底修复（重启不再丢配置）
- ⚡ 多协议 API 引擎（OpenAI / Anthropic 双协议）
- 📝 系统级提示词预设库
- 🐛 AI 打标 / 推送失败原因明细
- 🎨 窗口/设置菜单重构、侧边栏折叠、字号滑块等

### ✨ v1.1.0 更新
- 🌗 完整双主题（浅色 / 深色）一键切换
- 🔤 界面字号与工作区字号双轨独立控制
- ⚡ 正则脚本可视化编辑器
- 🤖 AI 模型名称配置 + 一键拉取模型列表

### ✨ 核心功能
- 📂 卡片库：PNG/JSON 解析、自动分类、批量管理
- 🔍 全局深度搜索（穿透世界书/触发词/标签）
- 🌍 世界书与正则脚本在线编辑
- 🏷️ 中英双语标签与分组系统
- 🌌 关系图谱（隔离/图例过滤/枢纽高亮/双击穿梭）
- 💬 聊天测卡（OpenAI / Anthropic 双协议 + 渲染/代码双模式）
- ⚡ Token 消耗实时估算
- 📦 单卡/批量整合包导出
- 🚀 一键推送至酒馆（本地物理拷贝）
- 🛡️ 保存快照 + 回收站 + 崩溃兜底

### 🛠 安装说明
- 双击安装包按向导安装（免管理员权限）
- 程序完全本地运行、无任何联网上传；若杀软误报请选择「允许/信任」
- 绿色免安装版：解压 win-unpacked 后直接运行「角色卡管理器.exe」
