> 🗄️ **已归档（2026-09-13 文档整理）**：本文件是历史原文，内容已抽取归并进新文档体系，此处仅作存档与细节回查。
> 归属：缺陷已并入 docs/bugs/BUG-性能与大库.md、BUG-架构与渲染.md、BUG-测卡工作区.md；性能数字已并入 docs/技术支持/技术数据-大库压测与性能.md

# 测试日志 · 桌面版 大库稳定性与加载性能专项（2026-09-13）

- **项目**：SillyTavern 角色卡管理器（桌面版，Electron + Vue3）
- **版本**：v2.2.6（工作区未发版，改动见「未发布」三段 CHANGELOG）
- **主测库**：`I:\03\角色色卡` —— 11,667 个文件 → 入库 **11,186 张**卡 / 9.76GB
- **对照库**：`E:\AI\酒馆工具\角色卡`（75 张，日常小库）
- **测试环境**：Windows / i7-12650H（10 核 16 线程）/ 31.7GB 内存 / NVMe
- **启动方式**：
  - dev：`npx vite --port 5173` + `VITE_DEV_SERVER_URL=http://localhost:5173` + `electron . --disable-gpu --remote-debugging-port=9338`
  - prod：`electron .`（无 Vite，走 `app://index.html` + `web/` 构建产物）
  - 隔离测量：`node scripts/measure-startup.mjs`（临时 profile，不碰真实配置）
- **自动化证据**：`scripts/library-dup-search-refresh.mjs`、`scripts/_probe-regex-ui.mjs`、`scripts/_cdp-eval.mjs`、`scripts/measure-startup.mjs`、`scripts/png-head-io-probe.mjs`
- **发版工具**：`scripts/release-check.mjs`（一条命令自查）＋ [`发布一条龙-执行清单.md`](发布一条龙-执行清单.md)

---

## 〇、今日总览（一句话版）

| 类别 | 内容 | 状态 |
|---|---|---|
| 🔴 Bug | 9 项：重复卡 / 刷新丢库 / 刷新 OOM / 加载越来越慢（写盘死循环）/ 索引零进展 / **窗口隐藏时索引停摆** / 正则「添加要切 Tab」+「删除无提示」+**首条新增漏刷新** / 日志刷屏 / 单测挂起 | 全部修复并回归 |
| 🚀 优化 | P0 去掉 PNG 卡重复搬运（worker 6.9→1.5s）/ P1 让步调度与闸门 / **P2 移除负优化内嵌缓存**（省 385MB~1.17GB 磁盘）/ P2b 日志降噪 / 写盘幂等（3415→0） | 均有同会话 A/B 数据 |
| 🧩 功能 | 桌面版测卡工作区落地（7 分区侧栏 + 引擎编排 + `chat_store`）+ **二期长期记忆通道**（`memory_store.json` + 7 个 IPC + 侧栏记忆库面板） | `npm test` 187/187 + 两套端到端 |
| 🧹 整理 | 文档索引 `docs/README.md`、参考稿归位 `docs/reference/`、临时产物/进程清理、6+3 个逻辑提交 | 工作区干净 |
| 🔬 复检 | 大库全量端到端复跑（§八）：全绿，**产品无新增缺陷**，修掉 4 处「测试自身」误报 | ✅ |
| ⏳ 未闭环 | ① `npm run build` + Release/OTA（需你确认）② 冷启动真机实测（需重启电脑） | 待执行 |

> ⚠️ **性能数字的读法**：同一份代码首屏可在 **16.0s ↔ 31.2s** 间波动（`fetch` 恒稳 2.5~3.4s，波动集中在
> CPU/内存密集的 `worker`/`assemble`；已采样确认非降频，属机器负载/内存带宽竞争）。⇒ 比较优化效果必须
> **同会话交替 A/B**；跨会话的绝对值只能当区间参考。详见 §3.2。

---

## 一、本轮改动清单

| # | 类型 | 改动 | 文件 |
|---|------|------|------|
| 1 | 🔴 Bug | 搜索后刷新「同一张卡重复出现」 | `js/utils/searchIndex.js`、`js/components/App.vue`、`test/searchIndex.test.mjs`(新) |
| 2 | 🔴 Bug | 扫描异常返回空数组 → 整库从界面消失 | `js/composables/useDiskScan.js` |
| 3 | 🔴 Bug | 刷新/搜索渲染进程 OOM 崩溃 | `js/utils/searchIndex.js` |
| 4 | 🔴 Bug | 每次启动重写数千张 PNG（加载 79.9s）+ 命中率低的写盘判据 | `js/composables/useCardCrud.js` |
| 5 | 🔴 Bug | 给「原本没有正则脚本」的卡新增第一条不刷新（Vue 3.4 computed 不传播） | `js/components/App.vue` |
| 6 | 🔴 Bug | 正则/状态栏删除无任何提示（改原生确认框） | `js/components/App.vue` |
| 7 | 🟡 Bug | 索引构建长时间零进展 / 窗口隐藏时永久停摆 | `js/utils/searchIndex.js`、`js/utils/tokenCache.js`、`js/components/App.vue` |
| 8 | 🟡 Bug | 载入期非角色卡 JSON 日志刷屏（385+ 条） | `js/composables/useCardCrud.js` |
| 9 | 🟡 Bug | `npm test` 跑完不退出（MessagePort 活跃句柄未 `unref`） | `js/utils/searchIndex.js` |
| 10 | 🚀 优化 P0 | PNG 卡不再白白多走两趟 structured clone | `js/composables/useCardCrud.js` |
| 11 | 🚀 优化 P1 | 让步调度统一 `yieldToMain` + 启动闸门 `waitForIdle` | `js/utils/searchIndex.js`、`js/utils/tokenCache.js`、`js/components/App.vue` |
| 12 | 🗑️ 优化 P2 | **移除** PNG 内嵌提取缓存（实测负优化）+ 一次性回收磁盘 | `main.js` |
| 13 | 🧪 工具 | 启动性能测量脚本（隔离 profile + 生产模式 + 分项解析） | `scripts/measure-startup.mjs`(新) |
| 14 | 🧪 工具 | PNG 头**读取量**探针（复刻主进程窗口逻辑，量化冷启动 I/O） | `scripts/png-head-io-probe.mjs`(新) |

---

## 二、Bug 明细与验证

### 🔴 BUG-1（严重）搜索后刷新，同一张卡重复出现

- **现象**：中文单字搜索 → 刷新 → 列表里同一张卡出现两次。
- **根因**：`searchIndex` 的 `build/buildAsync` **多轮全量重建重叠**：只在自己开头 `clear()`，既无代次保护，也无「同对象只索引一次」保证。刷新期间 `watch(library)` 被反复触发（数组引用替换 + 每张新解析卡 `triggerRef`）→ 旧对象在新构建 `clear()` 之后又被写回；同一对象被两个循环各 push 一次。**只有「精确 token」分支**会原样返回倒排桶 → 所以中文单字搜索最容易看到（英文多词走兜底分支自带身份去重）。
- **修复**：`generation` 代次号（在途构建自检退出）+ 索引幂等（`cards.has(card)` 直接 return）+ `search()` 结果 `[...new Set(results)]`；App 侧 `indexBuilding/indexDirty` 合并重建（在途只记一次补建）。
- **验证**：
  - 单测 `test/searchIndex.test.mjs` **7 项**（含「构建期间搜索返回上一代完整索引」双缓冲用例）✅
  - 大库脚本 `library-dup-search-refresh.mjs`：A~F 全阶段 `dup(lib)=0 / dup(list)=0`，库稳定 11,186 张 ✅
  - **生产模式**真实界面复测（搜索「龙」/「的」→ 点刷新 → 再搜）：可见列表 `dup=0` ✅

### 🔴 BUG-2（严重）刷新后整个大库从界面消失

- **根因**：主进程扫描 catch 分支返回 `{files:[], error}`，渲染层 `if (result && result.files)` 把空数组当真值 → `library.value = []`（磁盘未动）。这也解释了历史上「刷新中途只剩 478 张」的不完整计数。
- **修复**：先判 `result.error` 提前返回；库非空时**拒绝接受 0 文件结果**并提示。
- **验证**：连点刷新 ×6 库始终 11,186 张 ✅（脚本 F 阶段）

### ⚠️ BUG-3（严重，已缓解）刷新/搜索渲染进程 OOM

- **根因**：索引为每张卡常驻一份小写全文（11,186 卡 ≈531MB 字符，中文 UTF-16 更高）→ 加载完堆即 3.0GB / 上限 4.19GB，刷新重建再分配一份 + 并发重入 → `render-process-gone {reason:"oom", exitCode:-536870904}`。
- **修复**：索引不再保留文本（`_extractText` 按需复算）+ 单卡上限 `MAX_INDEX_TEXT=200000` + 重建合并。
- **验证**：堆基线 3039MB → **2013MB**；连点刷新 ×6 后 1893MB。**但压力场景（搜索+连续刷新叠加）峰值仍到 3.3GB** → 判定「已缓解，未根除」。

### 🔴 BUG-4（严重）库加载 79.9s —— 每次启动重写数千张 PNG

- **根因**：写盘判据看「**清洗后**的内存状态」；开启「🧹导入时忽略卡片自带标签」时每次加载都清空原生 tags → 永远判定需要物理清洗 → 写回 PNG → 下次启动又被当原生标签清掉 → **无限重写**（mtime 全变 → 刷新又全量重解析）。
- **修复**：改为按「**磁盘原有标签**」（`diskTagsBefore`）判定；仅当盘上确有该清掉的标签、或确有新标签要补时才写。
- **验证**：`3,415 张` → `971 张`（一次性残留清洗）→ **第 2 次启动 0 张**；本轮多次启动日志中已无写盘/自动打标行 ✅

### 🔴 BUG-5（严重）给「原本没有正则脚本」的卡新增第一条时列表不刷新

- **现象**：用户反馈的「添加正则/状态栏后反馈缓慢，必须切换选项卡才看到效果」在无脚本的卡上仍复现。
- **根因**：`regexScripts = computed(safeData → cardData)`。**Vue 3.4+ 的 computed 在「新值 === 旧值」时不再向下传播** —— `triggerRef(cardData)` 后 `safeData` 重算但**返回同一个对象**，依赖它的 `regexScripts` 不会被标脏：
  - 卡上**已有** `extensions.regex_scripts`：原地 `push`，computed 缓存的就是同一个数组对象 → `.length` 天然变化 → 侥幸正常（这就是上一轮 21→22 能通过的原因）；
  - 卡上**没有**该字段：新增会**整体替换数组** → 缓存里的旧空数组永不失效 → 面板恒显示「0 条脚本」。
- **修复**：新增 `cardContentVersion`，`refreshCardData()` 时自增，`regexScripts` 取值前先读它。
- **验证**（dev + CDP，卡「双瞳洗澡」，原本 0 条）：

| 步骤 | 结果 |
|---|---|
| 点「+ 立即新增一条正则脚本」（**不切任何 Tab**） | 徽标 **0 → 1 条**、DOM 行数 **0 → 1** ✅（修复前 0/0） |
| 点「🗑️ 删除」 | 枚举到原生窗口 `Pid \| #32770 \| 确认操作` ✅，此时行数仍为 1（未误删） |
| 发送 `WM_COMMAND/IDOK` | 行数 **1 → 0**、徽标 0 ✅（无需切 Tab） |
| 脚本 `_probe-regex-ui.mjs`（已适配 0 条 + 原生弹窗） | **PASS** ✅ |

### 🟡 BUG-7（中）索引/预热在窗口隐藏时永久停摆

- **根因**：分片让步只挂 `requestIdleCallback`；Chromium 在**窗口隐藏/最小化**时完全不回调，`setTimeout` 被节流至 ≥1s（后台 5 分钟后更狠）→ 224 个分片要跑几十分钟；`tokenCache.warmupAsync` 是**纯 idle**（永不回调）。启动闸门单独 `await requestIdleCallback(cb, {timeout})` 连 timeout 都不兑现（实测 **3 分钟仍未开始建索引**、搜索恒空）。
- **修复**：统一 `yieldToMain()`（前台 idle / 后台 `MessageChannel`，含定时器兜底）；闸门改 `waitForIdle()`。
- **验证**：隐藏窗口下 11,186 卡索引（349,552 词）+ Token 预热均正常完成 ✅

### 🟡 BUG-8/9/10（轻）

- BUG-8：载入期「非角色卡 JSON」日志刷屏 → 同因同类 1 条 + 封顶 40 条 + 收尾汇总（`跳过 385 个…已折叠 345 条`）✅
- BUG-9：`npm test` 挂起 → `MessageChannel` 仅在真实 DOM 创建 + Node 下 `unref()` ✅（178/178，5.2s 退出）
- BUG-10：磁盘 111 个 0 字节 `.tmp` + 1.17GB 缓存无人清理 → 缓存整体移除 + `cleanupLegacyEmbedCache()` 一次性回收（实测回收 **44 文件 / 758MB**）✅

---

## 三、性能数据

### 3.1 P0：PNG 卡不再重复搬运文本

| 阶段 | 优化前 | 优化后 |
|---|---|---|
| worker | 6.9s | 1.4~2.0s |
| assemble | 31s | 9.6~11.0s |

### 3.2 P2：内嵌提取缓存 A/B（同机交替、每次重启、同一大库）

| 方案 | fetch | worker | assemble | 首屏合计 | 缓存占盘 |
|---|---|---|---|---|---|
| **无缓存（现版本）** | 2.5~3.4s | 1.4~8.9s | 9.6~22.3s | **16.0~31.2s** | 0 |
| v1 `.json` 分片 | 9.3s | 2.0s | 11.0s | 36.9~43.3s | 1.17GB |
| v2 `.json.gz` 分片 | 29.1s | 1.6s | 52.8s | **85.3s** | 385MB |
| v8 二进制分片（过渡试验） | 62.5s | — | — | 137s | 385MB |

- 机理：PNG 头部提取内嵌 JSON 本身只要 ~3s（自适应窗口 + 128 路并发 + 按需流式，GC 可回收）；缓存把 1.2GB **对象**一次性常驻主进程 → 堆压力/GC 抖动 + 额外 IPC 克隆 → 慢 2~5 倍。`v8.deserialize` 产出 dictionary-mode 对象，跨 IPC 更慢。
- ⚠️ **重要观察：绝对值随机器状态大幅波动**。同一份代码、同一大库，首屏实测从 **16.0s 波动到 31.2s**：
  - 全程 `fetch` 恒稳 **2.5~3.4s**（I/O + 并发读 PNG 头，受环境影响小）；
  - 波动集中在 CPU/内存密集的 `worker` / `assemble`（同一版本 1.4s ↔ 8.9s）；
  - 采样确认**不是降频**：`%Processor Performance ≈ 190%`（有睿频）、整体 CPU 占用 20~33%，但 VS Code ×3 进程与 Defender（MsMpEng 累计 1300s+ CPU）在抢内存带宽；该类「组装 1.3GB 对象」的负载对内存带宽/竞争极敏感。
  - ⇒ **结论口径**：性能对比必须**同一次会话内交替 A/B**（本次缓存结论即如此得出，2~5 倍差距远超噪声）；跨会话的绝对值只能当参考区间。
- ✅ 移除缓存后**未再生成任何 `embed_cache_*`**，`.embed_cache_removed` 标记已写入（只扫一次目录）。

### 3.3 载入期日志

```
[载入] 跳过 385 个非角色卡/不可解析文件（同类日志已折叠 345 条）
```
（修复前逐条 `console.warn` 刷屏 385+ 行）

---

## 四、回归测试

| 项目 | 结果 |
|---|---|
| `npm test` | **178 / 178** 通过，5.2s，进程正常退出 |
| `npm run build:web` | `built in 2.07s` ✅ |
| 大库 dev 启动 | 11,186 张全部到位；`[profile] fetch=2.9s worker=1.5s assemble=9.9s`、首屏 16.0s |
| 大库 prod 启动（隔离 profile） | 首屏 17.7s，索引 ✅ Token 预热 ✅，无缓存文件 ✅ |
| `library-dup-search-refresh.mjs` | A~F 全阶段无重复、库稳定、连点刷新 ×6 无崩溃 |
| `_probe-regex-ui.mjs` | PASS（新增立即生效 / 删除未误删 / 原生确认框） |
| 写盘回归 | 多次启动日志中**无**「后台落盘自动打标」行 ✅ |

---

## 五、遗留与建议

1. **OOM 未根除**：压力场景峰值堆 3.3GB / 上限 4.19GB，需继续压（连续刷新 10+ 次、大库↔小库来回切、边打标边刷新）。
2. **冷启动（刚开机）已量化，但未真机实测**：

   `node scripts/png-head-io-probe.mjs` 实测（`I:\03\角色色卡`，库合计 10,072MB / 11,663 个 PNG+JSON）：

   | 指标 | 数值 |
   |---|---|
   | 每次启动读取量（复刻主进程窗口逻辑） | **约 9,565MB（9.3GB）** |
   | 文件打开次数 | **11,663 次** |
   | 热缓存实读耗时 | 2.3s（等效 4,206MB/s，即 `fetch≈3s` 的真相） |
   | 冷启动额外 I/O 估算 | 按 600MB/s 冷盘：≈**16~19s**；按 2~3GB/s 的 NVMe：≈**3~6s** |

   **为什么读这么多**：`chara` 内嵌块位于**文件末尾附近**（块结束偏移 P50≈616KB / P90≈897KB /
   P99≈1010KB），所以 ≤1MB 的卡（9,487 个，占大头）必然要整文件读；>1MB 的卡里还有 1,902 个的块
   不在前 1MB，只能走“读 8MB / 读整文件”兜底。
   ⇒ 原以为「把首读窗口缩到 64KB」能省 I/O 的设想**不成立**（64KB 只能命中 24%）。

   **推荐动作**：
   - 先实测确认痛不痛：**重启电脑后第一时间**执行
     ```
     node scripts/measure-startup.mjs --label 冷启动 --timeout 240000
     ```
     与热启动基线（首屏 16~31s、fetch 2.5~3.4s）对比。
   - 若冷启动 `fetch` 明显恶化（>10s），按性价比选：
     ① **尾读 + 标记搜索**：`chara` 块在文件末尾 → 只读文件**最后 ~256KB**，在缓冲里搜 `tEXt`/`chara\0`
        标记再解码（预估读量 9.3GB → ~3GB）。需重验 11k 卡全部提取正确（风险：块跨越尾窗、图像数据里撞标记）。
     ② **懒加载分片缓存**（不是旧方案）：只缓存「路径+mtime+size → 内嵌 JSON **文本**」，按需读单片、
        不把 1.2GB 对象常驻内存（当初 85.3s 的锅就在“对象常驻”，而不是“缓存”本身）。
     ③ 什么都不做：冷启动多花几秒，换掉 1.17GB 磁盘 + 一堆缓存维护代码（当前默认选择）。
3. **阈值未自适应**：`MAX_INDEX_TEXT=200000`、日志封顶 40 条目前写死。
4. **移动版（JSKZX）未同步**本轮修复（本文只覆盖桌面版）。
5. **未发版**：版本号仍 2.2.6，`CHANGELOG.md` 有三段「未发布」待合并为正式 release；`dist_new/` 未重建。

---

## 六、发版清单（v2.2.7）

### 已完成
- [x] `package.json` 版本 → **2.2.7**
- [x] `CHANGELOG.md`：三段「未发布」归档为 `v2.2.7（一）/(二)/(三)`，页首版本区间同步为 `v1.6.2 → v2.2.7`
- [x] `RELEASE_NOTES.md` 新增 v2.2.7 章节（修复/性能/回归）
- [x] 代码与文档已提交（本地 master，**未推送**）

### 待执行（发布动作，建议逐项打勾）
1. **冷启动复测**（可选但建议）：重启电脑后第一时间执行
   ```
   node scripts/measure-startup.mjs --label 冷启动 --timeout 240000
   ```
   与热启动基线（首屏 16~31s、fetch 2.5~3.4s）对比；若 `fetch` 明显恶化再评估分片缓存。
2. **出包**：`npm run build` → 产物在 `dist_new/`（`sillytavern-card-manager-2.2.7.exe` + `latest.yml` + `.blockmap`）
3. **安装包自测**：装 2.2.7 → 打开大库确认卡片数（11,186）、首屏就该在合理区间、无 `embed_cache_*` 残留、
   日志应出现一次「`[embed-cache] 已回收遗留缓存 N 个文件 / X MB`」（属预期迁移行为）
4. **打 tag 并推送**：`git tag v2.2.7` → `git push origin master --tags`
5. **建 Release**：在 `tian2418671-sys/JSKZX` 上传 exe + `latest.yml`（+ `.blockmap`）—— 缺 `latest.yml` 老客户端收不到 OTA
6. **OTA 验证**：在 2.2.6 客户端点「检查更新」应提示 2.2.7

### 回滚
- 发布前：`git reset --hard 9ea6b34`（v2.2.6 提交）即可；
- 发布后：新开一个 2.2.8 修正版，不要重发同号（OTA 以版本号比较）。

---

## 七、测卡工作区二期：长期记忆通道（2026-09-13 晚，已完成）

规格见 [`../桌面版测卡工作区-实现规格.md`](../桌面版测卡工作区-实现规格.md) §六。本次把该工程**最后一个未完成项**补齐。

### 7.1 问题

移动版长期记忆走 Android 原生 `MemoryPlugin`（SQLite），桌面版没有对应实现 → `chatBridge` 只能给**失败桩**，
侧栏「设置」分区的开关能保存、检索结果恒为空，UI 上明确写着「需二期补 `main.js` + `preload.js`」。

### 7.2 交付

| 层 | 内容 |
|---|---|
| 存储 | `main/memoryStore.js`（CommonJS，可单测）：`add/update/remove/clear/list/search/stats`；`type` = message(L1)/summary(L2)/fact(L3) |
| 落盘 | `userData/memory_store.json`，走既有 `atomicWriteJson`；存储层内部**串行化**写（实测并发调用 save 不重叠） |
| IPC | `memory:add/update/remove/clear/list/search/stats`（懒加载单例 + 统一异常包装，IPC 边界永不抛错） |
| 暴露 | `preload.js` 6+1 个方法；`chatBridge` 的 `MEMORY_METHODS` 同步加 `memoryStats`（非 Electron 仍走桩） |
| UI | 侧栏「设置」分区：记忆库条数 + 分类（事实/摘要/消息）+ 刷新 + **两段式二次确认清空** |

策略：同 `(type,key,content,cardName)` **去重合并**；每类保留最新 500 条；content 4KB/key 200B 上限；
检索用中文 2-gram + 英文词，按「命中率 × 类型权重（fact 3 / summary 2 / message 1）+ 时间新鲜度」排序；
`list/search` 返回**副本**（防止调用方改坏存储）。

### 7.3 验证

| 项目 | 结果 |
|---|---|
| `test/memoryStore.test.mjs`（新增） | **9 例**全绿：去重合并（时间戳刷新 + 返回副本）、空/超长内容规范化、按类型修剪（每类 3 条上限反例）、**落盘串行化**（断言并发 save 峰值 = 1）、update/remove/clear 契约、坏文件容错、2-gram 检索与 fact 优先、tokenize 边界 |
| `npm test` | **187 / 187**（基线 178 + 新增 9） |
| 侧栏端到端（生产构建，新增断言 H） | add ✅ list ✅ search ✅ **去重合并** ✅ stats `0 → 1` ✅ remove 后回到 `0`（`cleanedUp:true`）✅ |
| 引擎端到端（新增断言） | 写入 fact → `buildMemoryContext()` 产出含「记忆表格」的 system 片段 ✅；`removeMemory` 后该片段消失 ✅ |

### 7.4 顺带清理

- `ChatTestSidebar` 的 `mvuEnabled`/`ejsEnabled`/`segRenderEnabled` 三个 **死 prop**（引擎开关真源已是
  `chatStorage` 的 `getChatFlag`）与 EditorPanel 里对应的 `chat*Enabled` 绑定一起删除 —— 此前
  **每次渲染刷 3 条 Vue 告警**（`Property ... is not defined on instance`），清理后实测告警数 **0**。
- 两个端到端脚本的卡片选择改为「优先带世界书的卡，没有则退化为任意带封面的卡（世界书断言显式跳过）」，
  修掉小库下卡在 `no-card-row` / 误判失败的问题。

---

## 八、发版前全量复检（2026-09-13 深夜，最终一轮）

按「一条龙」流程重跑一遍全部自动化，**在真实大库 + 隔离 profile（不碰真实配置）**上执行：

| # | 项目 | 命令 | 结果 |
|---|---|---|---|
| 1 | 单元测试 | `npm test` | **187 / 187** 通过 |
| 2 | 前端构建 | `npm run build:web` | 成功（生产实例用它验证） |
| 3 | 大库 dev 启动 | 隔离 profile → `I:\03\角色色卡` | **11,186 张**、首屏 **17.7s**、日志 **0 条 Vue 告警** |
| 4 | 重复卡/刷新压测 | `library-dup-search-refresh.mjs` | **PASS**：A~F 全阶段 `dup(lib)=0 / dup(list)=0`；库稳定 11,186；连点刷新 ×6 无崩溃；堆峰值 **2756MB**（回落 1894MB） |
| 5 | 正则增删 UI | `_probe-regex-ui.mjs` | **PASS**：新增「0 → 1」**未切 Tab 立即生效**；删除→原生确认框、点后未误删 |
| 6 | 测卡引擎 | `chat-engine-test.mjs` | **PASS**：宏 11 键 / EJS / payload（无预设+有预设，含 Anthropic system 字段）/ 分段渲染 / 会话与 swipe / **记忆注入「记忆表格」**；世界书断言按「本卡无 `character_book`」正确跳过 |
| 7 | 测卡侧栏 | `chat-sidebar-test.mjs`（生产构建） | **PASS**：7/7 分区 + 预设下拉 11 项 + 旧头部清理 + `chatStore` 往返 + **记忆往返（去重合并/清理）** + 页脚「已恢复」 |

### 8.1 复检中修掉的 4 个「测试自身」缺陷（产品无缺陷）

复检暴露的问题**全部出在测试脚本**，逐个修正后脚本对「库数据差异」与「应用安全设计」不再误报：

| 脚本 | 误报 | 修法 |
|---|---|---|
| `_probe-regex-ui.mjs` | 大库下固定等 700ms 不够 → 报「未找到新增按钮（Tab 未展开？）」 | 改为**轮询等待**（最多 6s）直到正则分区真正渲染 |
| `chat-engine-test.mjs` | 首张 🌍 卡可能**没有内嵌世界书**（实测 `character_book: false`）→ 世界书断言误判失败 | 新增 `hasCharacterBook/bookEntries` 探测 + 自动换卡重试（最多 5 张）+ 无世界书时**显式标注跳过** |
| `chat-sidebar-test.mjs` | 抽屉停在「设置」分区时，侧栏自己的 API Key 输入框（`type=password`）被当成「旧头部没删干净」 | 头部断言**排除侧栏自身元素**；另把「记忆面板存在」升级为正式断言 |
| `chat-sidebar-test.mjs` | 卡片内容里的**外链图片**被应用 CSP 拦下（`img-src` 不放行 http(s)，防追踪像素）→ 被记为渲染错误 | 单列为 `cspImgBlocked` 计数，**不计入 errors**，并在输出里说明 |

> 结论：**上述均为测试口径问题，产品行为正确**（外链图拦截是既定的安全设计；无世界书的卡本来就不该注入世界书）。

### 8.2 复检结论

- 今日全部修复与优化**未发现回归**；产品侧**无新增缺陷**。
- 唯一未闭环的两项依旧是「与代码无关」的：#1 `npm run build` 打包 + Release/OTA（需你确认）、
  #2 冷启动真机实测（需重启电脑）。
- 新增发版工具：`scripts/release-check.mjs`（一条命令自查，本次输出「✅ 无阻塞项」）与
  [`发布一条龙-执行清单.md`](发布一条龙-执行清单.md)。

---

## 九、容量专项：P0 落地 + 堆构成审计 + P2 增量索引（2026-09-13 深夜）

数据与方法见 [`大库重复卡-压测数据记录.md`](大库重复卡-压测数据记录.md) §十三，此处只记改动与验证。

### 9.1 本轮改了什么

| 文件 | 改动 | 为什么 |
|---|---|---|
| `main.js` | 新增 `--js-flags=--max-old-space-size=6144 --expose-gc`（`appendSwitch` **和** `webPreferences.additionalArguments` 双写） | 实测 22k 卡加载期仍 `render-process-gone reason=oom`；只有 `additionalArguments` 才真正把参数交给渲染进程 |
| `js/utils/memoryGuard.js` | 新增 `assumedLimitMB`（App.vue 传 6144） | `performance.memory.jsHeapSizeLimit` 恒报 4192MB，阈值会算在假上限上（曾在真实 49% 水位误报 warn） |
| `js/utils/searchIndex.js` | ①`_tokenize` 改 `charCodeAt` 区间比较 ②新增跨代沿用 `carry`（`{reuse:true}`）③`remove()` 改用 carry token | ①原逐字符正则 → 22k 卡（待索引文本 ≈1.16GB）索引「建 10 分钟未完」 ②P1 砍正文后，未变动卡必须能不靠正文重建索引 ③同理免正文 |
| `js/composables/chat/chatStorage.js` + `useCardGroups.js` + `App.vue` | 新增 `migrateChatKeys` 并挂到「移动分组 / 分组重命名 / 换卡图」三处 | 测卡会话/变量树的键里嵌了卡片完整路径，路径一变就成孤儿 → 用户看到「会话凭空消失」（同类缺陷 2026-09-01 在覆盖层上踩过） |
| `scripts/capacity-check.ps1` | 新脚本（造副本→隔离 profile→启动→采样→刷新压测→清理） | 把 2 万卡压测从「手工」变一条命令；支持 `-ReplicaDir` 复用副本 |
| `scripts/_cdp-mem.mjs` / `_heap-audit.mjs` / `_cdp-text.mjs` | 新增/修正 | 堆、索引、应用自身进度三个探针 |

### 9.2 验证

| 项目 | 结果 |
|---|---|
| `npm test` | **202 / 202** 通过 |
| **新增** `test/searchIndexReuse.test.mjs`（7 例） | ✅ 增量 === 全量的等价性；含反例断言证明增量路径真被走到（`stats().reusedCards === 库大小`） |
| **新增** `test/chatKeyMigration.test.mjs`（4 例） | ✅ 会话键随路径迁移；目标已存在时不覆盖；目录前缀形式；无操作时数据原样 |
| `npm run build:web` | ✅ 通过 |
| 22k 真机（隔离 profile） | 加载完成 22,372 卡、堆 3,029MB、内存守门员正常采样、`window.gc` 可用（=js-flags 生效） |

**本轮抓到并修掉的三个真问题**（都是 22k 压测暴露的）：
① 渲染进程 OOM（上限没真正生效）② 索引分词病态慢 ③ 守门员阈值基准错误。
另修掉压测工具自身两个坑：`-Replica` 参数被内部 `$replica` 覆盖；CDP 取值双重 JSON 编码导致「not ready」误报。

### 9.3 🔴 更正：渲染进程堆上限**抬不上去**（实测否证）

第一次复测仍然 OOM（`crash.log` 13:30:04Z `reason=oom`），且堆只到 3.3GB —— 说明
`--max-old-space-size` 经 `js-flags`（`appendSwitch`）与 `webPreferences.additionalArguments` **两条路都无效**：

- `window.gc` 确实出现 → `--expose-gc` 生效；
- 但 `performance.memory.jsHeapSizeLimit` 恒为 4192MB，堆到 ~3.3GB 就 `reason=oom`
- ⇒ **Chromium 渲染进程不采纳该开关**，4192MB 就是真实上限。

因此：① `main.js` 撤销 `additionalArguments`（保留 `--expose-gc` 供守门员主动 GC），
注释里写明「勿重试」；② 守门员**回退**为按上报值（4192）判定（`assumedLimitMB` 仅留作逃生口）；
③ **真降内存只能靠 P1**。

### 9.4 P1a：大库正文懒加载（已落地）

`js/utils/cardSlim.js`（新增）+ App.vue 接线。刻意**收窄范围**——只丢两类：

| 丢弃 | 占全部文本 | 为什么可以丢 |
|---|---|---|
| 世界书词条 `content` | **1,163MB（80%）** | 列表/排序/搜索索引都不用；词条对象（keys/开关/位置）保留 → 🌍 徽标、注入、计数照常 |
| `alternate_greetings` | 120MB | 只有「打开某张卡」才需要 |

**保留** `description` / `first_mes` / `personality` / `extensions` 等 —— 它们被 AI 打标、查重差异比对、
Token 估算等**列表之外**的功能使用，砍了会让这些功能静默变差（故最初"整块正文都砍"的方案被否掉）。

关键设计：
- **原地压缩**（不替换 `item.data`）：全项目 ~40 处 `library.find(i => i.data === cardData.value)` 依赖对象身份
- **顺序不能反**：只在「索引建完 + token 预热完」之后压缩（`runTask` 内 `slimLibraryIfNeeded('index-built')`）；
  刷新时未变动卡靠 P2 的 token 沿用重建索引，所以压缩不会让搜索失效
- 打开卡片：`openFromLibrary` 改 async，先 `ensureCardFull`（按 path 用 `readEmbeddedBatch`/`readText` 读回），
  **读失败就拒绝打开**（否则编辑器空字段一保存就把卡写空）；带连点令牌防竞态
- 词条正文是「清空」而非删除 → `ensureCardFull` 逐条按 `comment/name` 配对填回（配不上回退同下标）
- 列表徽标/描述改用小字段（`_hasBook` / `_descShort`），排序用 `_tokens`
- 全局资产库（唯一需要全库正文的面板）→ **打开时后台逐批还原、关闭时重新压缩**（内存只在用户主动打开时付）

验证：`test/cardSlim.test.mjs` **8/8**（原地压缩 / 身份保持 / 无损还原 / V1 扁平卡 / 批量还原 / 读盘失败不破坏数据）；
`npm test` **214/214**；`build:web` ✅。

**22k 真机复测（同副本 / 隔离 profile）**：

| 指标 | 前 | 后 |
|---|---|---|
| 世界书词条正文 | 1,163 MB | **0 MB** |
| 纯文本合计 | 1,459 MB | **176 MB（-88%）** |
| 堆（GC 后） | 2,891 MB | **2,053 MB** |
| 加载 | 232s（含 OOM 重载） | **72s** |
| 索引 | 建 10 分钟未完 | 22,372 卡 / 349,648 token ✅ |
| **OOM** | 同位置崩 2 次 | **crash.log 无记录** ✅ |
| 守门员 | 从未触发 | `warns=1 gcCalls=1 releases=1`（真在上场） |





