# 🐞 BUG 记录总索引（docs/bugs/）

> **这里是本项目所有 BUG / 缺陷 / 历史坑的集中地。**
> 从各处的测试日志、压测记录、排查报告、交接手册中抽取归并，**按领域分 5 份详述**，本文件是总表。
> 最后整理：2026-09-22

---

## 一、怎么用

1. **动工前**：查本文件的总表 → 找到相关条目 → 进对应领域文档看「现象 / 根因 / 修复 / 验证」。
2. **修完新缺陷**：先在本文件加一行 + 在领域文档加一条完整记录，再写代码 —— 防止同类问题重复踩。
3. **要细节**：每条都标了「来源」（**版本 / 专项**）。过程数据与测量方法在 [`../技术支持/技术数据-大库压测与性能.md`](../技术支持/技术数据-大库压测与性能.md)，版本级技术细节在仓库根 `CHANGELOG.md`。

**编号规则**：`领域代号-序号`，序号按发现时间递增，**一经分配不再变更**。

| 代号 | 领域 | 文档 |
|---|---|---|
| `AR` | 架构 / Electron / Vue 运行时 / 测试调试 | [BUG-架构与渲染.md](BUG-架构与渲染.md) |
| `DF` | 数据 / 文件 / 字段口径 / 扫描落盘 | [BUG-数据与文件.md](BUG-数据与文件.md) |
| `AI` | AI 打标 / 本地向量 / 标签体系 | [BUG-AI打标与标签.md](BUG-AI打标与标签.md) |
| `PK` | 性能 / 大库 / 内存 / 索引 | [BUG-性能与大库.md](BUG-性能与大库.md) |
| `RL` | 发布 / 打包 / 更新 / OTA | [BUG-发布更新与打包.md](BUG-发布更新与打包.md) |
| `CT` | 测卡工作区（对话测试） | [BUG-测卡工作区.md](BUG-测卡工作区.md)（19 条） |

> **合计 141 条**（AR 48 ｜ **DF 22** ｜ AI 8 ｜ **PK 30** ｜ RL 13 ｜ **CT 20**）

**等级**：🔴 严重（崩溃/数据丢失/功能不可用） ｜ 🟡 中（功能异常但有绕法） ｜ 🟢 轻（体验/精度/日志）
**状态**：✅ 已修复 ｜ ⚠️ 已缓解未根除 ｜ ⬜ 未处理 ｜ 📌 设计如此 ｜ 🧩 非 BUG（缺口/取舍）

---

## 二、总表

### AR · 架构 / 渲染 / 测试调试（48 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| AR-01 | 🔴 | IPC 传 Vue 响应式 Proxy → `An object could not be cloned` | v1.0+ | ✅ |
| AR-02 | 🔴 | Electron 中 `window.prompt/confirm/alert` **静默失败** | v1.2 | ✅ |
| AR-03 | 🟡 | 未拦截 `window.open` / 新窗口 → 拖放文件被系统打开 | v1.5 | ✅ |
| AR-04 | 🔴 | `app://` 协议 handler 缺路径穿越校验 | v1.6 | ✅ |
| AR-05 | 🔴 | 路径白名单 `removeAllowedRoot` 无条件回收 → 误删库根 | v1.8.2 | ✅ |
| AR-06 | 🔴 | composables TDZ 回归（编译通过、运行崩溃） | v1.8.2 | ✅ |
| AR-07 | 🟡 | preload `on*` 事件多组件绑定互相清监听 | v1.2 | ✅ |
| AR-08 | 🟡 | 无 `electronAPI` 降级时卡在启动蒙版 | v1.2 | ✅ |
| AR-09 | 🔴 | Vue 弹窗放在 `#app` 闭合 `</div>` 之外 → 完全不编译 | v1.2 | ✅ |
| AR-10 | 🟡 | 组件注册名首字母连续大写 → kebab 标签解析不了 | v1.5 | ✅ |
| AR-11 | 🟡 | 纯 Options API 组件模板直接用模块级 import 函数 | v1.5 | ✅ |
| AR-12 | 🔴 | `shallowRef` 深层修改不触发响应式（`cardData`） | v1.7 | ✅ |
| AR-13 | 🔴 | `ctx` 漏暴露 → 模板访问 undefined 静默失效 | v1.8.3 | ✅ |
| AR-14 | 🔴 | watch 恢复期竞态：启动时用默认值覆盖用户配置 | v1.8.1 | ✅ |
| AR-15 | 🟡 | `renderHTML` 未转义 → 内容里的 `<html>` 被当 DOM 吞掉 | v1.2 | ✅ |
| AR-16 | 🟡 | `v-for` key 用 index → 删除/排序后错位 | v1.7 | ✅ |
| AR-17 | 🔴 | 启动 TDZ 崩溃（watch 时机早于解构） | v2.0 | ✅ |
| AR-18 | 🔴 | 正则/状态栏「新增首条不刷新」（Vue 3.4+ computed 值不变不传播） | v2.2.7 | ✅ |
| AR-19 | 🟡 | 正则/状态栏「添加要切 Tab、删除无任何提示」 | v2.2.7 | ✅ |
| AR-20 | 🟡 | `npm test` 跑完不退出（MessagePort 活跃句柄未 `unref`） | v2.2.7 | ✅ |
| AR-21 | 🔴 | Capacitor 插件代理当 Promise 解决值 → 状态栏主题永久失效【移动版】 | v1.10.21 | ✅ |
| AR-22 | 🟡 | `KeepAliveService` running 时序错乱 → 状态误报+反复拉起【移动版】 | v1.10.21 | ✅ |
| AR-23 | 🟡 | `.json` 插件格式化失效 | v2.2.5 | ✅ |
| AR-24 | 🟡 | dev 启动终端中文乱码 | v2.2.5 | ✅ |
| AR-25 | 🟡 | 导入打标双开关未彻底解耦 | v2.2.5 | ✅ |
| AR-26 | 📌 | 测试与调试的 4 个「自动化做不到 / 必须真机」的坑（技巧，非缺陷） | — | 📌 |
| AR-27 | 🔴 | 插件「效果」预览空白（缺宿主 DOM 官方挂载点） | v2.2.4 | ✅ |
| AR-28 | 🟡 | CSP 拦截插件预览脚本（改用独立 `app://` 内存路由） | v2.2.4 | ✅ |
| AR-29 | 🟡 | 字号滑块拖动时整个界面卡顿（全局响应式每帧 reflow） | v2.1.0 | ✅ |
| AR-30 | 🔴 | 卡内世界书词条增/删/克隆/排序后**列表不刷新**（computed 短路丢依赖 → `triggerRef` 叫不醒，切卡才出现） | v2.2.7 | ✅ |
| AR-31 | 🟡 | 往 `const ctx = {...}` 与 `provide('appCtx', ctx)` **之间**插代码 → AR-13 自动检查失配失效（报「结构已变化」而非「无问题」） | v2.2.13 | ✅ |
| AR-32 | 🔴 | 命令搬到注册表后仍引用**只定义在子组件内部**的状态 → `ctx.xxx` 为 undefined（菜单直接显示 undefined / 状态后缀静默消失） | v2.2.13 | ✅ |
| AR-33 | 🟡 | 多菜单命令在**非主菜单**里排序错乱（`listByMenu` 复用 `list()` 的顺序 → 按主菜单名落位、分组标题重复） | v2.2.13 | ✅ |
| AR-34 | 🟡 | 顶部菜单**点击命令后不关闭**（纯 CSS `:hover` 驱动 → 点击不改变 hover；命令弹窗时与菜单重叠） | v2.2.13 | ✅ |
| AR-35 | 🟡 | 批量操作悬浮条（z-50 + 渲染在所有弹窗**之后**）盖住弹窗——打标弹窗被遮「看不见」 | v2.2.13 | ✅ |
| AR-36 | 🟡 | 打标过程窗口被打标弹窗盖住（同 z-50、日志窗口先渲染）——「打标处理中…」全程看不到过程 | v2.2.13 | ✅ |
| AR-37 | 🟡 | 新增菜单「三处接线」漏一处（key 数组）→ 下拉渲染成空白；原 pycheck 两项恰好都查不到 | v2.2.13 | ✅ |
| AR-38 | 🟡 | 无启用/未保存收纳规则时点「重新扫描」**静默无反应**（界面零反馈）→ 用户误以为按钮坏了（引导缺失） | v2.2.13 | ✅ |
| AR-39 | 🔴 | 世界书「查看词条差异」必崩：`diffText` 为 `null` 时模板直读 `.masterLines`（**渲染期 TypeError**，词条数相同才不崩）+ `path` 裸 `split` | v2.2.13 | ✅ |
| AR-40 | 🔴 | 点「世界书库」/ **导入世界书** → **整个侧边栏消失**：模板调用 `wbEntryCount(wb)` 与 `selectWorldbook(wb)` 但**都未加入** `SidebarPanel` 的 setup return（AR-13 四步链的**第 4 步**漏项）→ 渲染期 TypeError 卸载组件。⚠️ 首次修复未生效：新加的静态检查器**自身失明**（`indexOf('</template>')` 只取第一个 → 7 个块中世界书分支不在待检文本；return 块里的**说明注释**被当成绑定键名；内联 CSS `repeat()/minmax()` 误报） | v2.2.13 | ✅ |
| AR-41 | 🟡 | 查重差异弹窗的**词条正文无任何着色**（纯文本直出）+ 通用分支两侧**各自独立滚动、行与行对不齐** + 切块算法**不保留位置**（同一句出现在别处即算「相同」）→ 长正文或小改动**肉眼看不出差异**。修：新增 `js/utils/textDiff.js`（行级 LCS 对齐 + 变更块内删/增**配对为 changed** + 行内 token 级精确高亮 + 超长降级保对齐），两侧共用同一份 `rows` 天然对齐 | v2.2.13 | ✅ |
| AR-42 | 🔴 | **进度条挂错了地方（设计跑偏）**：规格 TC-07 写的是「**上百本世界书查重**：有进度指示 + 当前项名」、§185「进度应挂到扫描阶段」，实现却挂到了**世界书库列表上方**（触发点是「打开世界书目录」）→ 浏览库时有进度条、**真正耗时的查重/版本对比反而全程无进度**；且查重**不重扫库**，「查重时扫描的进度」根本没有数据源。修：进度条移入**查重弹窗内部**（新增 `DedupeScanProgress.vue`，`WbDedupeModal`/`DedupeModal`/`ContentDedupeModal` 共用）、查重前**先重扫磁盘**（世界书复用 `wb:scan` 分批进度 = 真实百分比；角色卡 `refreshLibrary` 无进度通道 → 不定态滑动条，不编假数字）、浏览库改**日志反馈**；`wb:scan` 新增 `rescan` 选项（跳过新目录指纹验证，避免顶层无 `.json` 的库被误拒） | v2.2.13 | ✅ |
| AR-43 | 🔴 | 进度条**数字恒显示「0 / ?」**（而宽度正常推进）：`dedupeScanProgressForModal`（computed）**只定义在 setup 局部、未加入 setup return** → 模板拿到 `undefined` → 子组件回落默认值 `{done:0,total:0}`。因**宽度走 `scanPercent`（另一条路径）仍正常**，现象很隐蔽。同 AR-13 / CT-14 的 `_ctx.X` 一类病 | v2.2.13 | ✅ |
| AR-44 | 🟡 | 第二次查重时「**只看到进度条、结果却早已列出来**」：查重入口**先开弹窗再重扫**，但**没清空上一轮的 `groups`** → 扫描期间渲染的是**上一轮的陈旧结果**（用户看不出数据是旧的）。修：三个查重入口在扫描前 `groups = []` | v2.2.13 | ✅ |
| AR-45 | 🟡 | **查重进度条「反复横跳、时长时短，最后变成滚动的光条」**（用户实测原话）。三处叠加：① 查重是**多阶段**流程（重扫 → 逐本算指纹 → 比对），但**每个阶段的 `finally` 都 `resetDedupeScan()`** → 进度条**消失又出现**（「横跳」）；② `percent` 从 100 掉回 0（「时长时短」）；③ 内容查重阶段 2 置 `dedupeScanIndeterminate = true` → **切成滑动光条**（「最后变成滚动的光条」）。修：新增 **`finishDedupeScan()`**（统一收尾：推到 100% → 停留 400ms → 收起）+ 阶段 1 映射 **0~50%** / 阶段 2 继续 **50~100%**（**全程单调递增**）+ **全程确定态**（不切不定态）+ `rescanFor*Dedupe` 新增 `keepAlive` 选项（阶段切换时**不 reset**） | v2.2.13 | ✅ |
| AR-46 | 🔴 | **AR-45 二次报出（首次修复不彻底）**：进度条**有两条不同源的数据路径** —— **条宽**走 `dedupeScanPercent`（持续推进），**数字**走 `App.vue` 的 `dedupeScanProgressForModal` → **透传 `wbScanProgress`**（那是「扫描」的进度对象，**扫描开始/结束时会被置 `{done:0,total:0}`**）⇒ 扫描一结束就出现「**条宽 100%、数字却掉回 0 / ?、百分比整块消失**」= 用户看到的「又横跳」。⚠️ 与 AR-43（「宽度与数字走两条不同路径」）是**同一设计缺陷的变体**；旧探针**只采宽度**（没采数字文本）⇒ **5/5 通过是假绿**。修：新增**统一写入入口 `applyDedupeProgress()`**（`percent` 单调保护 + `done/total` 成对更新 + 收尾对齐 `total`，8 处裸赋值全部收敛）+ 新增 `dedupeScanDone/Total` 使**数字与宽度同源** + 阶段 2 补上数字推进 + 阶段 1 定时器跳过 `total<=0`（不再拽回 0） | v2.2.13 | ✅ |

### DF · 数据 / 文件 / 字段口径（22 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| DF-01 | 🔴 | 把 `card.id` 当路径用 → 文件操作全错 | v1.7 | ✅ |
| DF-02 | 🔴 | 世界书 `entries` 是对象字典（V2 老格式）→ 空屏崩溃 | v1.8.4 | ✅ |
| DF-03 | 🟡 | 保存/导出未剔除 `_` 前缀临时字段与 `uid` → 污染 JSON | v1.8.9 | ✅ |
| DF-04 | 🔴 | 用 blob URL 做图片持久地址 → 重启后破碎图标 | v1.6 | ✅ |
| DF-05 | 🔴 | 分类/标签未同步写原生 `data.tags` → 重启重扫丢失 | v1.7 | ✅ |
| DF-06 | 🔴 | 内嵌世界书 vs 库世界书**字段口径不同** → 转换后数据缺失 | v1.8.9 | ✅ |
| DF-07 | 🔴 | 世界书导出后**条目名字全部缺失**（`name`→`comment` 未映射，`insertion_order`→`order` 未回退） | v1.8.9 | ✅ |
| DF-08 | 🔴 | `refreshLibrary` 增量刷新**缺 path 去重** → 同一文件入库两条 | v2.2.7 | ✅ |
| DF-09 | 🔴 | `parseAndAddCard` 判重条件过窄（`opts.target &&` 短路）→ 同 path 进两次 | v2.2.7 | ✅ |
| DF-10 | 🟡 | `*.png.<pid>.<tid>.tmp` 外部残留被当**假卡入库**【移动版】 | v1.10.21 | ✅ |
| DF-11 | 🟡 | 快捷筛选「有世界书/有正则」恒显示 0 张（轻量条目字段口径）【移动版】 | v1.10.21 | ✅ |
| DF-12 | 🔴 | 子文件夹分支提前 `return` 跳过覆盖层恢复 → **标签重启后消失** | v2.1.0 | ✅ |
| DF-13 | 🟡 | `atomicWriteJson` 失败不清理 tmp → userData 积攒 95 个残留 | v2.1.0 | ✅ |
| DF-14 | 🔴 | 卡片保存路径未清洗（**DF-03 覆盖不全**）；且旧清洗规则「递归剔 `_` 前缀」会**误删第三方真实数据**（实测 7 类 131 处） | v2.2.7 | ✅ |
| DF-15 | 🔴 | 「从世界书库导入词条到角色卡」**不做字段口径转换**（库 `key/keysecondary/order/disable` 直接写进卡内嵌）→ 酒馆侧触发词失效 | v2.2.7 | ✅ |
| DF-16 | 🟡 | 分组列表累积大量**空组**（跨库历史配置 + 磁盘空文件夹残留），且**无批量清理入口**（预设空组无处可去） | v2.2.13 | ✅ |
| DF-17 | 🔴 | 世界书差异比对用「拼接大字符串」+ 词条无稳定主键（`uid` 跨源会重生成、**没有 `name`**）→ 看不出「哪个词条被增删」 | v2.2.13 | ✅ |
| DF-18 | 🔴 | 大世界书/预设被**静默丢弃**：5MB 闸门（世界书 + 预设两侧）+ 512KB 头部预检误杀 + **误杀被 `scanCache` 固化不自愈** | v2.2.13 | ✅ |
| DF-19 | 🟡 | 差异弹窗用 `Array.isArray(entries)` 判世界书 → 字典形态（V2 老格式）被判成**角色卡**，显示「设定完全一致」的**反向结论**（当前非活路径，顺手加固） | v2.2.13 | ✅ |
| DF-20 | 🔴 | **世界书懒加载从未成功过**：`file:readText` 返回 `{ success, text }` **对象**，`ensureWorldbookLoaded` 却把整个对象当字符串 `JSON.parse` → 抛 `"[object Object]" is not valid JSON`。**每次必失败**，PK-20 后大量书转懒加载才集中暴露（用户看到「疯狂报错弹框」）。⚠️ 一度用 `silent` 把弹框**盖住**（被用户批评「头疼捂嘴」）——报错是排查线索，不能掩盖；`silent` 只该用于「批量场景避免逐本弹框淹没界面」，且必须**汇总提示真因** | v2.2.13 | ✅ |
| DF-21 | 🔴 | **独立世界书的 ST 原生 `uid` 被无条件删掉**（DF-14 遗留待决项）。旧白名单把 `uid` 当「纯前端 v-for key」无差别剔除，但真实库取证（**804 个世界书 / 319,148 条词条**）显示 `entries[].uid` **100% 存在且 100% 是 ST 原生数字**（`0/1/2…`），本应用形态 **0 个**。🔬 **进一步取证**：uid **完全等于数组下标**（6 文件 11,430 条 **0 例外**）、真实库**恒为 `0..n-1`**（40 文件 0 缺失/0 空洞）⇒ **ST uid = 文件内顺序号，可重建**（修正此前「删真实数据」的过度解读）。⚠️ **5 版本未发现的三个原因**：① 症状不可见（ST 按顺序重建，行为一致，无人报）② v1.8.9 只验证了**角色卡内嵌** `character_book`（那里确实无 uid），而**独立世界书** 100% 有 ③ 注释里「本就无需持久化」**听起来合理** → 没人再验证前提。🛑 **状态：⏳ 方案待拍板** —— 当前实现（按形态区分：只删本应用生成形态）**引入了新问题**：调序后 `[0,1,2]`→**`[2,0,1]`**、删中间→**`[0,2]`**，**uid 与下标脱钩**。**推荐方案 A**（保存时按下标重写 `uid = i`，与 ST 完全一致且始终自洽，可删掉形态正则那套复杂度）；备选 B（恢复删除）；不推荐 C（维持现状）。方案对比见 [`../规格与计划/工作记录-20260923.md`](../规格与计划/工作记录-20260923.md) §二 | v2.2.13 | ⏳ **方案待拍板** |
| DF-22 | 🟡 | **JPEG 角色卡「导入能进、扫描不进」的不一致**（排查「角色卡跨版本查重方案」时发现，**与该方案无关的独立缺陷**）：**导入对话框**声明支持 JPEG（`HeaderBar.vue:20` 的 `accept=".png,.webp,.jpg,.jpeg,.json"`），且 `cardLoader.processFile` 对非 PNG 会走 `deepScanForJSON` 深度扫描 → **JPEG 卡确实能导入成功**；但**磁盘扫描白名单**只有 `.png` / `.webp` / `.json`（`main.js:1424` 与 `:4929`）→ **目录里的 JPEG 卡被静默跳过**（用户「打开目录」看不到，以为丢卡）。同一份数据两条路径行为相反 ⇒ 与 DF-17 / DF-19 同型的**口径不一致**。修（二选一，**待拍板**）：① 扫描白名单补 `.jpg`/`.jpeg`（需一并评估「目录里的普通照片会被深度扫描」的代价与假阳性风险，可能要接 `useSizeFilter`）；② 从导入对话框移除 `.jpg`/`.jpeg`（明确不支持）。⚠️ 真实卡库实测：43 张 PNG + 2 张 JSON，**0 张 JPEG** ⇒ 实际影响面小，但「声明支持却跳过」本身是缺陷 | v2.2.13 | ⬜ 待拍板 |

### AI · AI 打标 / 本地向量 / 标签体系（8 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| AI-01 | 🔴 | AI 打标时渲染进程崩溃（每打一张卡就全库重建索引） | v2.0 | ✅ |
| AI-02 | 🔴 | 三层漏斗互斥：规则命中就跳过向量层，向量模型永远用不上 | v2.1.0 | ✅ |
| AI-03 | 🔴 | 向量阈值与文本形态不匹配（0.65 → 0.35 + 标签展开） | v2.1.0 | ✅ |
| AI-04 | 🟡 | 向量模型下载链路三坑（hf-mirror RST UA / GitHub raw 极慢 / 100MB 分片） | v2.0 | ✅ |
| AI-05 | 🔴 | 「忽略自带标签」开关「时灵时不灵」（4 个消费点被绕过） | v2.1.0 | ✅ |
| AI-06 | 🔴 | 开关对**规则标签**无效 + 4 条提前 `return` 绕过物理清洗 | v2.2.1 | ✅ |
| AI-07 | 🟡 | 历史卡外来标签无法清洗（开关只对新导入生效） | v2.2.1 | ✅ |
| AI-08 | 🔴 | 自定义分类 key 同毫秒碰撞 → 标签归属错乱 | v2.2.1 | ✅ |

### PK · 性能 / 大库 / 内存 / 索引（30 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| PK-01 | 🔴 | 搜索后刷新，**同一张卡重复出现**（索引并发重叠重建） | v2.2.7 | ✅ |
| PK-02 | 🔴 | 刷新后**整个大库从界面消失**（扫描异常被当空库） | v2.2.7 | ✅ |
| PK-03 | 🔴 | 刷新/搜索**渲染进程 OOM 崩溃**（索引常驻全卡全文） | v2.2.7 | ⚠️ 已根治见 PK-10/P1a |
| PK-04 | 🔴 | 库加载 **79.9s**：写盘判据看错基准 → 每次启动重写数千张 PNG（无限循环） | v2.2.7 | ✅ |
| PK-05 | 🟡 | 索引/Token 预热在**窗口隐藏时永久停摆** | v2.2.7 | ✅ |
| PK-06 | 🟡 | 载入期「非角色卡文件」日志刷屏（385+ 行） | v2.2.7 | ✅ |
| PK-07 | 🟡 | PNG 内嵌提取缓存是**负优化**（慢 2~5 倍）+ 遗留缓存/`0 字节 .tmp` 无人清理 | v2.2.7 | ✅ |
| PK-08 | 🟡 | PNG 卡白白多走两趟 structured clone（万卡库 ≈1.2GB 搬运） | v2.2.7 | ✅ |
| PK-09 | 🔴 | 移动版刷新期间卡片重复出现（重扫未清空库）【移动版】 | v1.10.21 | ✅ |
| PK-10 | 🔴 | 渲染进程堆上限**抬不上去**（`js-flags` 不作用到渲染进程；`performance.memory.jsHeapSizeLimit` 不可作判据）+ 加载期仍 OOM | v2.2.7 | ✅ |
| PK-11 | 🔴 | `_tokenize` 逐字符跑正则 → 2 万卡库索引「10 分钟建不完」 | v2.2.7 | ✅ |
| PK-12 | 🟡 | 内存守门员阈值算在**假上限**（4192）上 → 误报 warn | v2.2.7 | ✅ |
| PK-13 | 🔴 | 打开某张卡**卡死 43~71 秒**（预设正则 × 界面巨脚本灾难性回溯） | v2.2.7 | ✅ |
| PK-14 | 🔴 | 瘦身卡（PNG）正文回读**必败**：回读传 `size: 0` 被主进程 `!size` 守卫拒读 → 窗口隐藏期间索引建完触发瘦身 → 切回后点卡弹「读取卡片正文失败」 | v2.2.7 补丁 | ✅ |
| PK-15 | 🔴 | 瘦身卡每次打开都 `triggerRef(library)` → shallowRef watch forceTrigger → **全库索引重建 + Token 预热 + 再瘦身**（连点几张卡明显卡顿） | v2.2.7 补丁 | ✅ |
| PK-16 | 🔴 | 瘦身完成后 `triggerRef(library)` → **第二轮全库索引重建**（启动双倍负载）且把索引覆盖成不含世界书正文的降级版 | v2.2.7 补丁 | ✅ |
| PK-17 | 🔴 | **PK-14 未修好**：未知 `size` 分支拿 `await fh.read()` 的**对象**当数字比较（`n < win`）→ 抛错被 catch 吃掉 → 仍恒返 `null`；实测 4/4 张卡 `size=0` 读不到、给真实 size 全成功 | v2.2.9 修订 / v2.2.10 | ✅ |
| PK-18 | 🔴 | 索引就绪后**短语校验整条被跳过**（`&& cardCount === 0`）→ 「系统」命中「体**系** 传**统**」的卡，**索引前后结果不一致**；顺带纠正「中文多字词退化为全表扫描」的流行误判 | v2.2.13 | ✅ |
| PK-19 | 🟡 | `_getMatches` 的**拉丁前缀**回退是 O(词表) 全表扫描（实测 150 万 token 上 19~36ms/次）；中文词不受影响 | v2.2.13 | ✅ |
| PK-20 | 🔴 | 世界书大库扫描：**主进程扫完了、渲染层拿不到** —— ≤50MB 世界书**无瘦身**，完整 `data` 单次 IPC 内联 → 501 本（3.56GB）**应用退出**；300 本超时挂住；200 本耗时暴增 13 倍。修：**累计内联预算**（`SCAN_INLINE_TOTAL_MAX_BYTES`，超预算只回元数据、正文按需懒加载）→ 501 本 **21.1s 完成、堆 224→324MB（7.7%）**，不再崩溃 | v2.2.13 | ✅ |
| PK-21 | 🔴 | 差异比对「**分散改动**」→ 降级策略按行号逐行比对，行数一变后面全错位 → 23745 行里 2 处真实改动被报成 **23540 行变更（假阳性 99.15%）**。修：改用**锚点对齐**（唯一行锚点 + LIS 保序 + 递归收敛到小段后精确 LCS）→ 假阳性 **99.15% → 0.01%**（放大 11771× → **1×**） | v2.2.13 | ✅ |
| PK-22 | 🔴 | 差异弹窗**卡顿**：整篇比对（23745 行）+ 397 个词条卡片一次性渲染 → **34,232 个 DOM 节点**、单次长任务 **750ms**、事件循环延迟 max **843ms**。修：整篇比对**默认折叠** + 长列表**分块渲染**（400 行/批）+ 词条卡片**分页**（30 个/页）→ DOM 降 **97%**（34,232 → 1,044）、首屏 **900ms → 24ms** | v2.2.13 | ✅ |
| PK-23 | 🔴 | 世界书大库**打开慢**：`wb:scan` 必须读完全部文件才能列出列表 → 1001 本 **36.0s**（5401 本 ≈150s）。修：**两阶段秒开** —— 阶段 1 只 `readdir`+`stat`（**完全不读内容**）+ 阶段 2 后台补元数据（`wb:meta` 通道 + 持久化缓存，二次 **0 次读盘**）→ **1001 本 83ms / 5401 本 237ms**（**提速 434×**），堆仅 15~19MB | v2.2.13 | ✅ |
| PK-24 | 🔴 | **秒开引入的回归**：阶段 1（`fastListOnly`）**只按 `.json` 后缀列出、不做 `isValidWorldbook` 校验** → 诱饵 JSON（角色卡/预设/大表格/损坏）**混进世界书列表**（实测 s5000：列出 **5401** vs 有效 **5001**，**400 个诱饵**），且 `wb:meta` 也不校验 → 诱饵还拿到书名与词条数。修：阶段 1 查 `scan_cache.worldbook` 的既有 `{mtime, valid}` 判定（**零额外读盘**）——`valid===false` 直接剔除；阶段 2 读文件时**顺带校验并回写缓存** + 渲染层移除无效项。「首次先快后准 / 二次精准且 0 次读盘」 | v2.2.13 | ✅ |
| PK-25 | 🟡 | 内容级查重的 **LSH 桶内两两比较是 O(bucket²)**：5401 本**极端同源**库（硬链接放大产物）下大量项落进同一批桶 → **亿级比对**，运行 **>25 分钟**未完。属**压力库构造特性**（真实库「5000 本几乎完全相同」不成立），但用户可能有「批量复制后改名」的历史库。修：**彻底弃用 MinHash+LSH，改 simhash**（位向量，整数异或+popcount 比较，**无桶概念**）→ 从根上消除 O(bucket²)；全量两两预筛 s5000 ≈ **12.5s** | v2.2.13 | ✅ |
| PK-26 | 🔴 | **秒开回归（第二处）：`wb.data` 变 `null`，所有读它的消费者静默算出 0**。PK-23 秒开把列表态 `data` 从「总是有」改成「可能没有」，但约 40 处消费者仍直接读 `wb.data.entries` / `wb.data.name`：查重词条数算 0、触发词重合度恒 **0%**（**反向结论，会误导用户误删正确的书**）、差异比对判不出是世界书、导出/复制/合并**产出空书**、词条 IDE 写 `data.entries` 直接 `TypeError` 崩溃、全库词条搜索恒空。修：消费点统一改「**优先轻量 `entryCount`/`wbName`，需要正文时才 `ensureWorldbookLoaded`**」 | v2.2.13 | ✅ |
| PK-27 | 🔴 | **批量读正文用 `Promise.all` 整组并发 → s5000 渲染进程被 OOM killer 杀掉**（**进程直接消失、无 `crash.log`**，典型内核杀进程）。同名查重按书名分组后**整组并发载入正文**：s1000 每组 40 本 ≈ 528MB（**侥幸存活**）→ s5000 每组 **200 本 × 13.2MB ≈ 2.6GB**（**被杀**）。根因是**数据供给架构**：扫描时 `entriesArr` 已在内存，却只把 `{mtime,size,name,entryCount}` 写缓存（`main.js:2855`）—— **触发词被丢掉**，下游查重只能反复重读 34.8GB。修（止血）：新增**唯一**批量读正文入口 `consumeWorldbookBodies()`（**顺序消费** + 每本用完即释放 + 每 10 本让出主线程 + 逐本失败不中断）；根治方案见 [`../规格与计划/世界书大库-加载与查重架构方案.md`](../规格与计划/世界书大库-加载与查重架构方案.md) | v2.2.13 | ⚠️ 已止血，根治待拍板 |
| PK-28 | 🟡 | **内存守卫只有「事后水位」、缺「读前预估」** → 超大书只能「读了才知道爆」（PK-27 那次是**内核直接杀进程**，连提示的机会都没有）。架构方案 §5.3 长期标「⚠️ 待写死」。修：`memoryGuard.js` 新增 `PARSE_SIZE_FACTOR=2.2` / `estimateParseBytes` / `preflightRead`（判定顺序：**单本超限 → 总量超「余量×0.7」→ 拿不到 `readMemory` 就降级用假定上限**，不抛错）；守门员加 `.preflight()`（**复用同一 `readMemory`**）+ `stats.preflights/preflightRejects`；接到 `consumeWorldbookBodies` 的**并发路径**滑动窗口之前（**顺序路径不预检** —— 峰值≈单本，天然安全）。单测 +6（542 全绿） | v2.2.13 | ✅ 已修复 |
| PK-29 | 🔴 | **内容级查重把无关世界书判为重复**（用户实测：「两完全不相似的世界书进行对比查重」）。两个独立缺陷叠加：① **simhash 阈值 T=19 失准** —— S0.5 的「负样本 min 31」是在**合成样本**上测的，真实库无关对距离**低至 17**；② **Union-Find 传递链误聚** —— A~B≤19、B~C≤19 但 **A~C=29**，并查集仍并成一簇，弹窗只显示「与推荐版距离」⇒ 按钮「清理其余」**可一键误删**。真实库 32 本取证：18 对候选中 **10 对纯误判**（最严重者距离 19 但真实 Jaccard **0.1%**）；阈值扫描 `T≤16` 零误报零漏报、`T=19` 误报 10 对。⚠️ 旧探针只断言「有没有分组」不校验「分组是否正确」= **假绿**（同 AR-46 型）。修：阈值 → **16** + **MinHash 复核闸门**（`estimateSimilarity ≥ 0.85`）+ **簇心校验** + 弹窗给可验证依据 | v2.2.13 | ✅ |
| PK-30 | 🔴 | **MinHash 哈希族完全退化** → 相似度估计严重失真（真实 Jaccard **0.2%** 却估计 **68.8%**，误差 68.5%，而 96 维理论标准误仅 ≈10.2%）。根因：`h = h*31 + c` 对**等长** shingle（本项目 4-gram 恒 4 字符）时，seed 只贡献**线性偏移** `seed*31^len` ⇒ **不改变 shingle 间相对顺序** → 96 个「独立」哈希函数实际只有 1 个。取证：300 个等长 shingle × 96 seed → 「最小值落点」仅 **2 种**取值（期望 96）、被选中最多者 **95 次**、两两排序一致率 **100%**（期望 50%）。影响：角色卡/预设查重 + 所有 `estimateSimilarity` 展示值不可信（PK-29 的「真实 0.1% 显示 70%」有它一份贡献）。修：改 **FNV-1a 异或 + 雪崩混合**（修正版实测 argmin 79/300、一致率 50.1%）。⚠️ 教训：**「看起来随机」≠「真的随机」**——用了 5 个版本没发现，因为**从没人校验过哈希族本身**。凡多哈希函数族（MinHash/LSH/布隆过滤器）必须单独验证函数间独立性 | v2.2.13 | ✅ |
| AR-47 | 🟡 | **角色卡查重「全程滚动光条」**（不定态泄漏）。`rescanForCardDedupe()` 因角色卡重扫无进度通道置 `dedupeScanIndeterminate=true`，而 `startDedupeScan()` **重扫后从不复位** → 之后「算 Token / 读文件信息 / 组装分组」这些**有真实进度**的阶段全在不定态下跑。真实库（11,188 张）重扫约 10s，用户全程只见滑动光条（AR-38「零反馈 = 坏了」）。修：重扫结束立刻切回确定态 + 后续阶段补真实进度（0~90% / 90% / 95~99% / 100%）+ 同步循环改 `for` 且每 N 项让出主线程。⚠️ 顺带修：**新流程开始的重置不能走统一入口**（其 `Math.max` 会保留上一轮残留百分比 → 实测 `I61` 闪跳）→ 约定「重置用裸赋值、流程内推进走入口」 | v2.2.13 | ✅ 已修复 |
| AR-48 | 🔴 | **同名查重「聚错组」→ 对比弹窗两侧是完全无关的两张卡，且可一键误删**。同名查重**只按卡内 `name` 聚类**，真实库大量 `name` 撞车但内容无关：实测 **1910 组中 350 组（18.3%）**组内最大相似度 < 20%（很多 = 0%）。用户实际那组：描述 2990 字 vs 256 字，**两卡 `name` 竟都是 `"1"`**（直读 PNG 元数据核实）→ 弹窗渲染成「同一角色两个版本」，按钮却是「保留此版，**清理其余**」⇒ **一键误删真卡**。⚠️ 比对逻辑本身没错（正确显示 2990 vs 254 字）——**是上游分组错**，修比对是治错了病。修：① 加 simhash「仅名称相同」判定（**T=24**，实测同名同源 p50=0 / 无关 p50=31，间隙极大；成本 4547 张 ≈ 2.0s）；② 弹窗标注组标题 + 红边角标 + 可验证依据 + 按钮降级；③ 清理二次确认**列出文件名警示**。世界书侧同类问题一并修（复用已在手的 L1a Jaccard，< 5% 判 `_nameOnly`，零额外成本） | v2.2.13 | ✅ 已修复 |

### RL · 发布 / 打包 / 更新（13 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| RL-01 | 🔴 | `latest.yml` 未上传 → **OTA 静默失败（404）** | v1.8.5 | ✅ |
| RL-02 | 🟡 | `electron-builder` 卡住=正在下载 Electron 二进制（勿误杀）+ 离线打包要点 | v1.8.5 | ✅ |
| RL-03 | 🟡 | 打包前未杀旧实例 → EBUSY 锁文件 | v1.8.9 | ✅ |
| RL-04 | 🟡 | `build:web` 的 `rmSync` 可能没真正清空 `web/`（旧产物进 asar 撑体积） | v1.8.9 | ✅ |
| RL-05 | 🟡 | 发布未同步 GitHub Release（`releases/latest`）→ OTA 误报 downgrade | v1.8.5 | ✅ |
| RL-06 | 🟢 | `gh release` 无输出 = 正在后台上传（勿中断）；`gh release list` 看 Latest | v2.2.7 | 📌 |
| RL-07 | 🔴 | `electron-builder` 报 **EPERM rmdir onnxruntime-node\lib**（输出目录被占用） | v2.2.7 | ✅ |
| RL-08 | 🟡 | 本机 git 直连 GitHub **间歇性** `Empty reply from server` → 推送失败 | v2.2.7 | ✅ |
| RL-09 | 🟡 | PS 5.1 `Invoke-WebRequest` 弹安全确认 / `Content` 返回 `byte[]` | v2.2.7 | ✅ |
| RL-10 | 🟢 | `gh release view --jq` 只接受**一个**输出表达式 | v2.2.7 | ✅ |
| RL-11 | 🟡 | 从 VS Code 终端启动打包后的 GUI → 应用继承控制台，终端 `^C` **连坐杀死应用** | v2.2.7 | 📌 |
| RL-12 | 🟡 | BOM-less `.ps1` 含中文字面量 → 乱码；`Start-Process` 两个重定向**不能同文件** | v2.2.7 | ✅ |
| RL-13 | 🟡 | 打包/校验脚本里的中文参数与超长单行命令在终端被随机 `^C` | v2.2.7 | 📌 |

### CT · 测卡工作区（20 条）

| ID | 等级 | 缺陷 | 版本 | 状态 |
|---|---|---|---|---|
| CT-01 | 🔴 | 侧栏渲染中断：模板对不可信列表直接取 `.length` → 整个测卡 Tab 被卸载 | v2.2.7 | ✅ |
| CT-02 | 🔴 | 读存储的 `computed` **永久缓存** → 选了预设引擎永远读不到（静默失效） | v2.2.7 | ✅ |
| CT-03 | 🟡 | 测试自身陷阱：CDP 里 `import` 模块会**另建一份实例** → 读数全错 | v2.2.7 | ✅ |
| CT-04 | 🟡 | 预设条目不可见、不可操作 | v1.10.21 | ✅ |
| CT-05 | 🟡 | 嵌套格式 `prompt_order` 使条目开关**形同虚设** | v1.10.21 | ✅ |
| CT-06 | 🟢 | 温度参数显示精度丢失（`0.85` 显示成 `0.8`） | v1.10.21 | ✅ |
| CT-07 | 🟢 | 侧边栏标签栏溢出，「设置」被裁掉 | v1.10.21 | ✅ |
| CT-08 | 🟢 | 变量重命名后键位移到对象末尾（引擎无 `move` 指令） | v1.10.21 | ✅ |
| CT-09 | 🔴 | 项目此前**完全不维护 `prompt_order`** → 预设开关不生效 | v2.2.6 | ✅ |
| CT-10 | 🔴 | 测卡键未随物理路径迁移 → 移动/重命名/换卡图后**会话与变量树消失** | v2.2.7 | ✅ |
| CT-11 | 🧩 | 移动版测卡引擎移植的 3 处阻塞级缺口（import 失效 / `useChatPresets` 落后 / `useStatusbarPreview` 分叉） | v2.2.7 | ✅ 已补 |
| CT-12 | 📌 | 自动化坑：重复点击已激活 Tab 不会 remount | v2.2.7 | 📌 |
| CT-13 | 🔴 | 测卡区**状态栏永远空白**（三层：CSP 拦 srcdoc 内联脚本 → 缺 `Vue/z/_` 全局 → 缺 `getCurrentMessageId/updateVariablesWith` 宿主 API） | v2.2.7 | ✅ |
| CT-14 | 🟡 | 测卡区**翻页点了没反应**（开场白只取 `first_mes`、没读附加问候语 → 恒单候选；且模板 `ctx.*` 不可见 → 点击即抛 `Cannot read …`） | v2.2.7 | ✅ |
| CT-15 | 🟡 | 「清空记录」按钮一直是死的（同类根因：模板访问不到 setup 局部的 `ctx`） | v2.2.7 | ✅ |
| CT-16 | 🟡 | 卡内面板顶部/底部各露出一行「```」（裸围栏包完整文档时 `fenceLooksLikeHtml` 不认 `<!DOCTYPE html>` → 围栏被当正文带进 iframe） | v2.2.9 修订 / v2.2.10 | ✅ |
| CT-17 | 🟡 | 长期记忆「无法删除」：逐条删除入口根本不存在（`removeMemory` 从未被调用，侧栏只有清空全部）→ 记忆 v4.1 移植轮补查看器 + 每行 🗑 | v2.2.11 | ✅ |
| CT-18 | 🟡 | 测卡聊天记录无法逐条删除（引擎无 deleteMessage）→ 引擎补 API + 每条气泡加 🗑 | v2.2.11 | ✅ |
| CT-19 | 🔴 | 卡内 HTML 面板缺 jQuery/Vue 全局库 → `$ is not defined`（`buildHtmlSrcdoc` 的「仅 `<body>` / 纯片段」两个回退分支漏拼 vendor，CT-13 修复未覆盖全部分支） | v2.2.12 | ✅ |
| CT-20 | 🟡 | **测卡区不识别外链界面（loader）** → `$('body').load('URL')` / `<iframe src>` 的卡面板**完全不显示**，而**同一张卡**在卡编辑器预览面板里却能显示。根因：两处**分类器口径不一致** —— `useStatusbarPreview.classifyTemplate` 早有 `loader` 分类 + `EditorPanel` 有渲染分支，而 `useChatRender.segmentMessage` **只产出 text/html 两类**、`htmlNeedsIframe` 也只看 `<style>/<script>/<html>` ⇒ loader 内容要么进 srcdoc（远程 URL 不加载）、要么当纯文本。修：① 新增 `loaderUrlOf()`（与预览面板**同口径**；**只认 `http(s)://`**）；② 三处产出 `loader` 段（loader > html > text）；③ `ChatPanelSeg.vue` 加 loader 分支（`<iframe :src>` 直载）。单测 +8 | v2.2.13 | ✅ |

---

## 三、历史修复速览（v1.0 ~ v2.0，来自会话时间线）

> 下列条目来自交接手册的会话时间线，均为**已修复**的历史缺陷（详细到代码级的部分已归类进各领域文档）。
> 保留此表是为了「某个功能的某个怪现象，是不是以前修过」这类追溯。

| 领域 | 版本 | 修复 |
|---|---|---|
| AR | v1.2 | 弹窗位置、`prompt/confirm` 静默失败、IPC 传 Proxy |
| AR | v1.5 | 组件注册名大小写；纯 Options API 组件模板里用模块级 import 函数 |
| AR | v1.6 | 360 沙箱拦截：GPU 沙箱（`disable-gpu-sandbox` 已入源码）、渲染进程沙箱（`--no-sandbox` **只在用户桌面快捷方式加参，不改源码** —— 用户明确要求） |
| AR | v1.8.1 | 排序物理时间（mtime/birthtime）；异步竞态守卫；双协议 `max_tokens`；`js/main.js` → `js/entry.js` 改名 |
| AR | v1.8.2 | 快照前缀互串修复（`escapeRegExp` + `snapshotRe` + `isSnapshotOf` 精确匹配） |
| AR | v1.8.3 | 关系图谱 ctx 漏暴露（`exportGraph` / `graphStats` / `graphBuilding`）；收编 IPC 克隆错误（reactive 数组 JSON 剥离） |
| AR | v1.8.4 | 导入 JSON 卡空屏崩溃（`extractBookEntries` 脏形态全链路防御） |
| AR | v1.8.20 | 关系图谱卡顿 v3/v4（连线预算、超大群体跳过、头像限流、位置种子、构建缓存） |
| DF | v1.6 | **Win10 21H2 导入不了任何卡片**（IPC 白名单 → 改用浏览器 File API `rawBuffer/rawText` 绕过）；jpg 卡片支持；破碎图标（blob URL → `local-file://`） |
| DF | v1.7 | 物理文件夹分组（移动=物理移动文件）；删除卡片自动清理空分组 |
| DF | v1.8.1 | 全局标签池受 sanitize 开关控制；自动分类不建「幽灵分组」；`pngParser` iTXt 偏移；标签批量删除 |
| DF | v1.8.2 | 换卡图功能（sharp + `card:replaceImage` IPC + 结构校验） |
| DF | v1.8.9 | 世界书库条目名字/触发词**显示混乱**（UI 层：名字主显示 + 🔑 触发词副显示）；名字输入框标签改「📝 名称 / 备注 (Comment)」 |
| DF | v2.0 | `seenPaths` O(1) 去重 + 流式批量拉取；`triggerRef` 补漏；`pngParser` 大卡兜底；`tokenEstimate` 超长文本防护 |
| PK | v1.8 | 千卡库性能大修（主进程扫描异步化、自动打标 I/O 风暴治理、分块加载、Token 缓存、Schwartzian 排序） |
| PK | v1.8.1 | 自动打标 I/O 风暴治理 |
| PK | v2.0 | AI 打标崩溃（`pendingRebuild` 跳过索引重建）；三层 O(n²) `find` → O(1) Map；LLM 层索引 bug；崩溃兜底 `crashReporter` |
| CT | v1.10.21 | 变量树渲染式重写；预设条目删/增/克隆；类型色块与统计徽章 |
| CT | v2.2.6 | 预设缝合中心的条目批量操作、常用条目库、自定义条目 |

---

## 四、缺陷来源（按版本 / 专项）

> 各条目末尾的「来源」标注的是**发生时的版本与专项**；原始排查过程记录已在文档重组时**并入本体系**（数据进 `技术支持/`、结论进本目录），不再单独保留旧文件。

| 版本 / 专项 | 时间 | 贡献了哪些条目 |
|---|---|---|
| v2.0 向量与打标专项（含早期交接手册的坑清单） | 2026-08 | AR-01~AR-16、AR-26、DF-01~DF-06、AI-01、AI-04、RL-01~RL-06 |
| v2.1.0 持久化 / 漏斗 / 交互性能专项 | 2026-09-01 | AR-29、DF-12、DF-13、AI-02、AI-03、AI-05 |
| v2.2.1 外来标签清洗 + AI 归类转正 | 2026-09-04 | AI-06、AI-07、AI-08 |
| v1.10.21 移动版专项（Android 测卡 / 变量树 / 导入） | 2026-09-12 | AR-21、AR-22、DF-10、DF-11、PK-09、CT-04~CT-08 |
| v2.2.7 桌面版大库稳定性与加载性能专项 | 2026-09-13 | PK-01~PK-08、PK-10~PK-12、AR-18~AR-20、CT-09、CT-10、RL-05、RL-07~RL-13 |
| v2.2.7 全库「同类重复项」排查 | 2026-09-13 | DF-08、DF-09 |
| v2.2.7 测卡工作区落地 + 二期长期记忆 | 2026-09-13 | CT-01~CT-03、CT-11、CT-12 |
| v2.2.7 补丁（状态栏渲染 / 翻页控件） | 2026-09-14 | CT-13、CT-14、CT-15 |
| v2.2.9 修订 + v2.2.10 重发（同内容，换号只为走 OTA） | 2026-09-14 | PK-17、CT-16（另含界面瘦身，非缺陷） |
| v2.2.5 / v2.2.4 插件工作区与代码编辑器 | 2026-09 | AR-23~AR-25、AR-27、AR-28 |
| 更早版本（详见 `CHANGELOG.md` 与第三节「历史修复速览」） | v1.0 ~ v2.0 | AR-17、DF-07 |

**细节去哪查**：过程数据与测量方法 → [`../技术支持/技术数据-大库压测与性能.md`](../技术支持/技术数据-大库压测与性能.md)；版本级技术细节 → 仓库根 `CHANGELOG.md`；测卡规格与计划 → [`../规格与计划/`](../规格与计划/README.md)。

---

## 五、已知非缺陷（勿重复排查）

> 这些现象**看起来像 BUG，实际是设计如此或环境限制**。历史上被重复排查过，列出来省时间。

| 现象 | 结论 |
|---|---|
| 导入**同名卡**被跳过、没覆盖 | **设计如此**：`importCardFiles` 明确「同名跳过，绝不覆盖」 |
| 快捷筛选/列表里某些计数为 0 | 先看 [DF-11](BUG-数据与文件.md)（轻量字段口径），再看是否只是索引/筛选未就绪 |
| `KeystorePlugin` 被注册两次 | Capacitor **内部行为**，无功能影响（移动版） |
| 模拟器/真机测试卡住、`adb` 一直 offline | SDK **路径含中文**导致 emulator 内部乱码挂死 → 用 ASCII 路径 junction 规避（移动版） |
| `BestClock: Missing network time fix` | 模拟器无网络时间同步，**与 App 无关** |
| ECharts 关系图谱节点点击无法自动化 | zrender 分层渲染、位置不可靠 → **只能手动验证** |
| 长按动作菜单在 CDP/adb 下触发不了 | Android WebView 合成器独立处理手势 → **只能真机手动验证** |
| `vite build` 后 `web/` 里还有旧文件 | `build:web` 才清 `web/`；只跑 `vite build` 不清理 |
| 重复点击已激活的 Tab 没反应（自动化） | 侧栏只在聊天 Tab 挂载时创建 → 测试须「先切走再切回」（见 [CT-12](BUG-测卡工作区.md)） |
| 页面里的外链图片不显示 | 应用 CSP 拦截 http(s) 图片（防追踪像素），**设计如此** |

> `CHANGELOG.md` 与 `docs/规格与计划/**` 是**活文档**，缺陷条目在它们里也会同步出现（分工：那里写「做了什么」，这里写「坏在哪、怎么修」）。
