# 📊 技术数据 — 大库压测与性能

> 本文汇总**所有实测数字**（桌面版为主，含移动版对照）。
> 缺陷的「现象 / 根因 / 修复」见 [`../bugs/BUG-性能与大库.md`](../bugs/BUG-性能与大库.md)；本文只放**可比较的数据与测量方法**。
> 数据来源：`docs/history/测试日志-2026-09-13.md`、`docs/history/大库重复卡-压测数据记录.md`、`docs/history/测试日志-2026-09-12.md`
> 测量日期：2026-09-13

---

## 一、测试环境与基准库

| 项 | 值 |
|---|---|
| 机器 | Windows / i7-12650H（10 核 16 线程）/ 31.7GB 内存 / NVMe |
| 主测库 | `I:\03\角色色卡` —— 11,667 文件 → 入库 **11,186 张卡 / 9.76GB**（53 个一级分组，11,142 PNG + 525 JSON） |
| 容量库 | `I:\03\_20k-test` 副本 —— **22,276 PNG / 19.85GB / 入库 22,372 张**（脚本造副本，测完删除） |
| 对照小库 | `E:\AI\酒馆工具\角色卡`（75 张） |
| 隔离方式 | `--user-data-dir=<临时目录>`（**绝不碰真实配置**，只改 profile 里的 `lastFolder`） |
| 启动方式 | dev：`npx vite --port 5173` + `VITE_DEV_SERVER_URL` + `electron . --disable-gpu --remote-debugging-port=9338`；prod：`electron .`（`app://index.html` + `web/` 产物） |
| 取样口径 | 内存先 `HeapProfiler.collectGarbage` 再读 `performance.memory`；耗时取渲染端启动日志 |
| 崩溃判据 | `%APPDATA%\sillytavern-card-manager\crash.log`（或隔离 profile 下同名文件）是否有新增 |

> ⚠️ **性能数字的读法（重要）**：同一份代码首屏可在 **16.0s ↔ 31.2s** 间波动。`fetch` 恒稳 2.5~3.4s，
> 波动集中在 CPU/内存密集的 `worker` / `assemble`。采样确认**不是降频**（`%Processor Performance ≈ 190%`），
> 而是 VS Code ×3 进程 + Defender 在抢内存带宽。⇒ **比较优化效果必须同会话交替 A/B**，跨会话的绝对值只能当区间参考。

---

## 二、加载分项耗时（渲染端日志口径）

```
[profile] 解析分项: fetch=…ms worker=…ms assemble=…ms 总文件=…
[startup] 蒙版淡出(总耗时): …ms        ← 用户可感知的「首屏就绪」
```

| 库规模 | 首屏合计 | fetch（主进程批读 + PNG 内嵌提取） | worker（`JSON.parse` 纯文本卡） | assemble（组装/血统鉴定/打标） |
|---|---|---|---|---|
| 11,186 卡 / 9.76GB | **16.0 – 31.2s** | 2.5 – 3.4s | 1.4 – 8.9s | 9.6 – 22.3s |
| 22,372 卡 / 19.85GB | **85 – 117s** | 4.8 → 8.2s | 68.2 → 28.5s | 103.4 → 48.5s |

**优化前后（同一大库、同会话交替）**

| 项目 | 优化前 | 优化后 | 说明 |
|---|---|---|---|
| worker | 6.9s | **1.4 ~ 2.0s** | PNG 卡不再多走两趟 structured clone |
| assemble | 31s | **9.6 ~ 11.0s** | 同上 |
| 首屏（写盘风暴修复） | **79.9s** | **41 ~ 43s** | 每次启动重写 3,415 张 PNG → 0 张 |
| 首屏（P0+P2 全量） | — | **16.0 ~ 17.0s** | 见 §四 缓存 A/B「无缓存」行 |

**写盘收敛（幂等性验证）**

| 轮次 | 重写卡数 | 主进程扫描 | fetch | worker | assemble | 蒙版淡出 |
|---|---|---|---|---|---|---|
| 修复前 | **3,415** | 2.4s | 9.3s | 18.3s | 58.3s | **79.9s** |
| 修复后·第 1 次 | 971（真残留清洗，一次性） | 2.4s | 8.1s | 6.1s | 26.9s | **40.6s** |
| 修复后·第 2 次 | **0** | 2.8s | 11.8s | 6.6s | 19.0s | **41.8s** |
| 修复后·最终回归 | **0** | 2.7s | 6.9s | 7.2s | 31.0s | **43.3s** |
| 22k 库·首次 | 16,161 | — | — | — | — | 116.8s |
| 22k 库·二次 | **858** | — | — | — | — | ≈85s |

**载入期日志**（P2b 降噪后）
```
[载入] 跳过 385 个非角色卡/不可解析文件（同类日志已折叠 345 条）
```

---

## 三、堆构成审计（22,372 卡 / 19.85GB 副本，GC 后取样）

| 项 | 数值 | 占比 |
|---|---|---|
| **堆占用** | **2,891 MB**（浮动垃圾仅 7MB） | 100% |
| 纯文本（字符数 × 2 字节） | **1,459 MB** | 50% |
| └ **世界书词条正文** | **1,163 MB** | **占全部文本的 80%** |
| └ `alternate_greetings` | 120 MB | — |
| └ `first_mes` / `description` | 70 MB / 63 MB | — |
| └ 其余（`mes_example` / 系统提示 / 性格 / 场景 / 原生标签 / 杂项） | 合计 ≈ 43 MB | — |
| 非文本（对象 / 键名 / 数组 / 倒排索引） | **1,432 MB** | 50% |
| 内嵌世界书 / 词条总数 | **16,340 本 / 429,144 条** | — |

**结论**
- 大头是**世界书词条**（429k 条对象的正文 + 对象开销），**不是卡本体**；
- 搜索索引**不保留文本**，中文按**单字**建倒排 → 唯一 token 只有几千级，索引本身不是大头；
- ⇒ 内存优化的正确方向是「**列表态不常驻词条正文**」（已做，P1a），下一刀（P1b）应瞄准**词条对象本身与倒排索引规模**。

---

## 四、P1a 正文懒加载：落地前后对比（22,372 卡 / 同一副本）

| 指标 | 改造前 | 改造后 | 变化 |
|---|---|---|---|
| 世界书词条正文 | 1,163 MB | **0 MB** | ✅ 100% 释放 |
| `alternate_greetings` | 120 MB | **0 MB** | ✅ |
| 纯文本合计 | 1,459 MB | **176 MB** | **-88%** |
| 堆（GC 后） | 2,891 MB | **2,053 MB**（脚本单次采样 1,906MB） | **≈ -29%** |
| 加载耗时 | 232s（含 OOM 重载） | **72s**（同副本、热 cache） | ✅ |
| 搜索索引 | 曾「建 10 分钟未完」 | 22,372 卡 / **349,648 token**，建完 ✅ | ✅ |
| **渲染进程 OOM** | 同位置 **2 次崩溃** | **`crash.log` 无记录** | ✅✅ |
| 内存守门员 | 从未触发 | 24k 卡高压下 `warns=1 gcCalls=1 releases=1` | ✅ 真在上场 |

- 日志：`[slim] 已压缩 19114 张卡的正文（index-built，保留字段：名称/标签/token/有无世界书）`
  —— 其余 3,258 张本来就没有词条正文可放（`slimCard` 无内容可释放时不标 `_slim`）。
- 剩余 2,053MB 构成 = 对象/键/倒排索引（**1,877MB**）+ 文本 176MB。
- **刷新压测**：日志出现第二次扫描（13.6s）→ 刷新确实在 3.5GB 堆上跑完重建，**未崩**。
  （探针报 `refresh 0s` 是误读：`__jskDiag.refresh()` 返回的是"发射后不等待"的包装。）

### 搜索索引规模

| 指标 | 值 |
|---|---|
| 索引卡数 | 22,372 |
| 唯一 token | **349,648**（11k 库为 349,552 —— 中文单字倒排决定上限） |
| 单卡索引文本上限 | `MAX_INDEX_TEXT = 200000` |
| 跨代沿用（P2）附加开销 | 22k 卡约 **+70MB**（`key = path\|mtime\|size` → token 数组，token 复用 Map 的 key 对象） |
| 重建合并效果 | `watch: 80 → coalesced: 77 → start: 4 → done: 3` |

---

## 五、PNG 内嵌提取缓存 A/B（同机交替、每次重启、同一大库）

| 方案 | fetch | worker | assemble | 首屏合计 | 缓存占盘 |
|---|---|---|---|---|---|
| **无缓存（现版本）** | 2.5 ~ 3.4s | 1.4 ~ 8.9s | 9.6 ~ 22.3s | **16.0 ~ 31.2s** | 0 |
| v1 `.json` 分片（24 片） | 9.3s | 2.0s | 11.0s | 36.9 ~ 43.3s | 1.17GB |
| v2 `.json.gz` 分片（23 片） | 29.1s | 1.6s | 52.8s | **85.3s** | 385MB |
| v8 二进制分片（过渡试验） | 62.5s | — | — | **137s** | 385MB |

**机理**：从 PNG 头部提取内嵌 JSON 本身只要 ~3s（自适应窗口 + 128 路并发 + 按需流式，GC 可回收）；
缓存把约 1.2GB 的**对象**一次性常驻主进程 → 堆压力/GC 抖动 + 额外 IPC 克隆，反而慢 2~5 倍。
压缩只减磁盘与读取量，**消不掉"对象常驻"这个根因**。
`v8.deserialize` 产出 **dictionary-mode** 对象，跨 IPC structured clone 与属性访问都明显变慢 —— 要交给渲染进程的数据，`JSON.parse` 反而是快路径。

**处置**：删除整块实现（-173 行）+ `cleanupLegacyEmbedCache()` 一次性回收（本机 **44 文件 / 758MB**）+ `.embed_cache_removed` 标记保证只扫一次目录。

---

## 六、冷启动 I/O 量化（`scripts/png-head-io-probe.mjs`）

| 指标 | 数值 |
|---|---|
| 库合计 | 10,072 MB / 11,663 个 PNG+JSON |
| **每次启动读取量**（复刻主进程窗口逻辑） | **约 9,565MB（9.3GB）** |
| 文件打开次数 | **11,663 次** |
| 热缓存实读耗时 | 2.3s（等效 4,206MB/s —— 即 `fetch≈3s` 的真相） |
| 冷启动额外 I/O 估算 | 按 600MB/s 冷盘 ≈ **16~19s**；按 2~3GB/s NVMe ≈ **3~6s** |

**为什么读这么多**：`chara` 内嵌块位于**文件末尾附近**（块结束偏移 P50≈616KB / P90≈897KB / P99≈1010KB），
所以 ≤1MB 的卡（9,487 个，占大头）必然整文件读；>1MB 的卡里还有 1,902 个块不在前 1MB，只能走「读 8MB / 读整文件」兜底。
⇒ 「把首读窗口缩到 64KB 省 I/O」的设想**不成立**（64KB 只能命中 24%）。

**22k 库对账**：22,276 张 PNG 需读 ≈19.8GB，11k 库实测 9,565MB 翻倍正好对上。

**可选优化（未做）**
| 方案 | 预估 | 风险 |
|---|---|---|
| ① 尾读 + 标记搜索（`chara` 在末尾 → 只读最后 ~256KB 搜 `tEXt`/`chara\0`） | 读量 9.3GB → ~3GB | 块跨越尾窗、图像数据里撞标记 |
| ② 懒加载分片缓存（只缓存「path+mtime+size → 内嵌 JSON **文本**」，按需读单片） | 省重复提取 | 不要把对象常驻（这正是 85.3s 的锅） |
| ③ 什么都不做 | 冷启动多花几秒 | 换掉 1.17GB 磁盘 + 一堆缓存维护代码（**当前默认选择**） |

---

## 七、容量边界与安全上限

| 库规模 | 加载 | 堆占用（加载 + 索引后） | 判定 |
|---|---|---|---|
| 11,186 卡 / 9.76GB | 16.0 – 31.2s | 2.0 – 3.0GB（48 – 73%） | ✅ 日常可用 |
| **22,372 卡 / 19.85GB** | **85 – 117s**（P1a 后 72s） | **3.05 – 3.26GB / 4.19GB（73 – 78%）**，P1a 后 2,053MB | ⚠️ **能加载**（全部入库、索引可用、无崩溃），但「余量小」 |

**结论**：
- 2 万卡「能加载」，不是打不开；但处于「能跑、余量小」区间；
- **安全上限建议按 1.2–1.5 万卡规划**（对应堆 < 70%、首屏 < 60s）；
- 渲染进程堆上限 **4,192MB 是真的、抬不上去**（详见 [PK-10](../bugs/BUG-性能与大库.md)），所以只能靠减少占用，不能靠抬上限。

---

## 八、稳定性压测（11,186 卡）

| 阶段 | 操作 | 耗时 | total | dupPaths | dupIds | 判定 |
|---|---|---|---|---|---|---|
| A | 基线 | — | 11186 | 0 | 0 | ✅ |
| B-1 / B-2 | 单路刷新 | 68.2s / 21.3s（第 1 轮）· 85.9s / 17.5s（第 2 轮） | 11186 | 0 | 0 | ✅ 首次含冷缓存/自动打标落盘 |
| C | **连点刷新 ×10** | **26.7s / 30.7s** | 11186 | 0 | 0 | ✅ 合并生效（≤2 次实跑，而非 10×17s≈180s） |
| D | 并发全量加载 ×2 | — | — | — | — | ⏸ 被外层超时切断（非崩溃） |
| E | 搜索 → **不等防抖**立即刷新 | — | 11186 | 0 | 0 | ✅ |
| F | **连点刷新 ×6（搜索态）** | — | **11186** | 0 | 0 | ✅ 堆峰值 1,893MB |

**崩溃对比**：修复前 `crash.log` 新增 `oom / -536870904`（F 段连点刷新后触发自动 reload，界面库变 0）；修复后**全程无新崩溃条目**。

### 未完成 / 未做
| 项 | 状态 |
|---|---|
| 搜索 ×10（分组 all） | ❌ 未跑（脚本每轮对 11k 条做全量 Map 遍历，超时） |
| D 并发全量加载 ×2 | ⏸ 两轮都被外层超时切断 |
| 修复前的对照复现 | ❌ 未做（「并发重入是唯一根因」未经对照实验证实，但因果链已在 [PK-03](../bugs/BUG-性能与大库.md) 记录） |
| 22k 的「刷新」压测 | ⚠️ 曾被 `[vite]` 整页刷新打断（创建 VS Code 任务改写了 `.vscode/tasks.json` 触发 HMR full reload，`crash.log` 无记录）→ 结论**待补测** |
| 冷启动真机实测 | ⬜ 需**重启电脑后第一时间**跑 `node scripts/measure-startup.mjs --label 冷启动 --timeout 240000` |

---

## 九、移动版性能对照（Android，500 卡 / 103MB）

| 场景 | scan | read | parse | publish | cache | total |
|---|---|---|---|---|---|---|
| coldscan（删缓存冷启动） | 11,006ms | 5,332ms | 1,199ms | 3ms | 825ms | **17,912ms** |
| refresh #1（热重载） | 8,404ms | 1,144ms | 1,209ms | 4ms | 163ms | **11,306ms** |
| refresh #2（写缓存） | 8,905ms | 1,572ms | 1,532ms | 6ms | 7,065ms | **12,696ms** |
| refresh #3（重装后，当前基线） | 7,638ms | 1,166ms | 1,120ms | 4ms | 229ms | **10,887ms** |
| **热重载**（`restoreLibraryFromCache`） | — | — | — | — | — | **<500ms** |

- 设备：Android 15 (API 35) 模拟器 `JSKZX_Test`，1080×2400 @420dpi，x86_64 + WHPX
- 数据：500 文件 / 53 目录 / 约 103MB → 有效卡 **364 张**（136 被负缓存排除 / 29 无 chara / 其余格式不支持）
- 稳定性：快速滚动 80 次 11ms 无崩溃；连续切筛选 8 次 2ms；`publish < 10ms`
- 缓存：`.jskzx_cache.json` 1.38MB（含 `_lb` / `_rx` 轻量标记，250 张有世界书 / 48 张有正则）

---

## 十、复现命令速查

```powershell
# 一键容量压测（自动造副本；-ReplicaDir 复用已有副本，注意参数名不是 -Replica）
powershell -File scripts\capacity-check.ps1 -Copies 2 -Port 9338 -Label 22k -Keep -Hold -TimeoutSec 900

# 启动分项（隔离 profile，生产模式）
node scripts/measure-startup.mjs --library "I:\03\角色色卡" --runs 2 --label 11k
node scripts/measure-startup.mjs --label 冷启动 --timeout 240000      # 重启电脑后第一时间

# 冷启动 I/O 量化
node scripts/png-head-io-probe.mjs

# 重复卡 / 刷新压测
node scripts/library-dup-search-refresh.mjs
node scripts/_cdp-mem.mjs --port 9338 --refresh --gc
node scripts/_heap-audit.mjs
```
